// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.EntityHistoryBlotterRowDtoDaoRenderer

package org.maiaframework.toggles

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.search.AgGridSearchModel
import org.maiaframework.domain.search.SearchResultPage
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.SqlParams
import org.maiaframework.jdbc.search.AgGridSearchModelConverter
import org.springframework.stereotype.Repository
import tools.jackson.databind.json.JsonMapper


@Repository
class FeatureToggleHistoryBlotterRowDtoDao(
    private val jdbcOps: JdbcOps,
    private val jsonMapper: JsonMapper
) {


    private val dtoRowMapper = FeatureToggleHistoryBlotterRowDtoRowMapper(jsonMapper)


    private val searchModelConverter = AgGridSearchModelConverter(
        FeatureToggleHistoryBlotterRowDtoMeta::fieldNameToColumnName,
        FeatureToggleHistoryBlotterRowDtoMeta::fieldNameToJdbcType
    )


    fun search(entityId: DomainId, searchModel: AgGridSearchModel): SearchResultPage<FeatureToggleHistoryBlotterRowDto> {

        val sqlParams = SqlParams()
        sqlParams.addValue("entityId", entityId)
        val entityIdFilter = "toggles.feature_toggle_history.id = :entityId"
        val whereClause = this.searchModelConverter.buildWhereClauseFor(searchModel.filterModel, sqlParams, entityIdFilter)
        val offsetAndLimitClause = this.searchModelConverter.buildOffsetAndLimitFor(searchModel)
        val orderByClause = this.searchModelConverter.buildOrderByClause(searchModel)

        val sqlForTotalCount = """
            select count(*)
            from toggles.feature_toggle_history
            $whereClause
            """.trimIndent()

        val sqlForPage = """
            select
                toggles.feature_toggle_history.activation_strategies as activationStrategies,
                toggles.feature_toggle_history.attributes as attributes,
                toggles.feature_toggle_history.change_type as changeType,
                toggles.feature_toggle_history.comment as comment,
                toggles.feature_toggle_history.contact_person as contactPerson,
                toggles.feature_toggle_history.description as description,
                toggles.feature_toggle_history.enabled as enabled,
                toggles.feature_toggle_history.feature_name as featureName,
                toggles.feature_toggle_history.info_link as infoLink,
                toggles.feature_toggle_history.last_modified_by_name as lastModifiedByUsername,
                toggles.feature_toggle_history.last_modified_timestamp_utc as lastModifiedTimestampUtc,
                toggles.feature_toggle_history.review_date as reviewDate,
                toggles.feature_toggle_history.ticket_key as ticketKey,
                toggles.feature_toggle_history.version as version
            from toggles.feature_toggle_history
            $whereClause
            $orderByClause
            $offsetAndLimitClause
            """.trimIndent()

        val totalResultCount = this.jdbcOps.queryForLong(sqlForTotalCount, sqlParams)
        val results = this.jdbcOps.queryForList(sqlForPage, sqlParams, this.dtoRowMapper)
        val endRow = searchModel.endRow
        val limit = if (endRow == null) null else (endRow - searchModel.startRow)

        return SearchResultPage(
            results,
            totalResultCount,
            searchModel.startRow,
            limit
        )

    }


    fun count(entityId: DomainId, searchModel: AgGridSearchModel): Long {

        val sqlParams = SqlParams()
        sqlParams.addValue("entityId", entityId)
        val entityIdFilter = "toggles.feature_toggle_history.id = :entityId"
        val whereClause = this.searchModelConverter.buildWhereClauseFor(searchModel.filterModel, sqlParams, entityIdFilter)

        val sqlForTotalCount = """
            select count(*)
            from toggles.feature_toggle_history
            $whereClause
            """.trimIndent()

        return this.jdbcOps.queryForLong(sqlForTotalCount, sqlParams)

    }


}

