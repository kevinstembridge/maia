// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.gen.testing.jdbc.sample.non_surrogate_pk

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.gen.sample.types.SomeStringValueClass
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository


@Repository
class NonSurrogateIdPrimaryKeyHistoryDao(
    private val fieldConverter: NonSurrogateIdPrimaryKeyHistoryEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = NonSurrogateIdPrimaryKeyHistoryEntityRowMapper()


    private val primaryKeyRowMapper = NonSurrogateIdPrimaryKeyHistoryEntityPkRowMapper()


    fun insert(entity: NonSurrogateIdPrimaryKeyHistoryEntity) {

        jdbcOps.update(
            """
            insert into testing.non_surrogate_id_primary_key_history (
                change_type,
                created_timestamp_utc,
                id,
                some_modifiable_string,
                version
            ) values (
                :changeType,
                :createdTimestampUtc,
                :id,
                :someModifiableString,
                :version
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("changeType", entity.changeType)
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("id", entity.id.value)
                addValue("someModifiableString", entity.someModifiableString)
                addValue("version", entity.version)
            }
        )

    }


    fun bulkInsert(entities: List<NonSurrogateIdPrimaryKeyHistoryEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into testing.non_surrogate_id_primary_key_history (
                change_type,
                created_timestamp_utc,
                id,
                some_modifiable_string,
                version
            ) values (
                :changeType,
                :createdTimestampUtc,
                :id,
                :someModifiableString,
                :version
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("changeType", entity.changeType)
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("id", entity.id.value)
                    addValue("someModifiableString", entity.someModifiableString)
                    addValue("version", entity.version)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from testing.non_surrogate_id_primary_key_history",
            SqlParams()
        )

    }


    fun count(filter: NonSurrogateIdPrimaryKeyHistoryEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from testing.non_surrogate_id_primary_key_history
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(id: SomeStringValueClass, version: Long): NonSurrogateIdPrimaryKeyHistoryEntity {

        return findByPrimaryKeyOrNull(id, version)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    NonSurrogateIdPrimaryKeyHistoryEntity::class.java,
                    mapOf(
                        "id" to id,
                        "version" to version,
                    )
                ),
                NonSurrogateIdPrimaryKeyHistoryEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(id: SomeStringValueClass, version: Long): NonSurrogateIdPrimaryKeyHistoryEntity? {

        return jdbcOps.queryForList(
            "select * from testing.non_surrogate_id_primary_key_history where id = :id and version = :version",
            SqlParams().apply {
            addValue("id", id.value)
            addValue("version", version)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: SomeStringValueClass, version: Long): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from testing.non_surrogate_id_primary_key_history where id = :id and version = :version",
            SqlParams().apply {
                addValue("id", id.value)
                addValue("version", version)
           }
        )
       
        return count > 0
       
    }

    fun findAllBy(filter: NonSurrogateIdPrimaryKeyHistoryEntityFilter): List<NonSurrogateIdPrimaryKeyHistoryEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from testing.non_surrogate_id_primary_key_history where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: NonSurrogateIdPrimaryKeyHistoryEntityFilter): Sequence<NonSurrogateIdPrimaryKeyHistoryEntityPk> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id, version from testing.non_surrogate_id_primary_key_history where $whereClause",
            sqlParams,
            this.primaryKeyRowMapper
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<NonSurrogateIdPrimaryKeyHistoryEntityPk> {

        return this.jdbcOps.queryForSequence(
            "select id, version from testing.non_surrogate_id_primary_key_history;",
            SqlParams(),
            this.primaryKeyRowMapper
        )

    }


    fun findAllBy(filter: NonSurrogateIdPrimaryKeyHistoryEntityFilter, pageable: Pageable): List<NonSurrogateIdPrimaryKeyHistoryEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from testing.non_surrogate_id_primary_key_history where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<NonSurrogateIdPrimaryKeyHistoryEntity> {

        return this.jdbcOps.queryForSequence(
            "select * from testing.non_surrogate_id_primary_key_history;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


}

