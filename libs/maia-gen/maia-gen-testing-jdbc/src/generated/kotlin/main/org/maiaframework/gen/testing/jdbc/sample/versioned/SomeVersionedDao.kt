// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.gen.testing.jdbc.sample.versioned

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.domain.persist.FieldUpdate
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.MaiaRowMapper
import org.maiaframework.jdbc.ResultSetAdapter
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository
import java.sql.PreparedStatement


@Repository
class SomeVersionedDao(
    private val fieldConverter: SomeVersionedEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = SomeVersionedEntityRowMapper()


    private val primaryKeyRowMapper = MaiaRowMapper { rsa -> rsa.readDomainId("id") }


    fun insert(entity: SomeVersionedEntity) {

        jdbcOps.update(
            """
            insert into testing.some_versioned (
                c_ts,
                id,
                some_int,
                some_string,
                v
            ) values (
                :createdTimestampUtc,
                :id,
                :someInt,
                :someString,
                :version
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("id", entity.id)
                addValue("someInt", entity.someInt)
                addValue("someString", entity.someString)
                addValue("version", entity.version)
            }
        )

    }


    fun bulkInsert(entities: List<SomeVersionedEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into testing.some_versioned (
                c_ts,
                id,
                some_int,
                some_string,
                v
            ) values (
                :createdTimestampUtc,
                :id,
                :someInt,
                :someString,
                :version
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("id", entity.id)
                    addValue("someInt", entity.someInt)
                    addValue("someString", entity.someString)
                    addValue("version", entity.version)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from testing.some_versioned",
            SqlParams()
        )

    }


    fun count(filter: SomeVersionedEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from testing.some_versioned
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(id: DomainId): SomeVersionedEntity {

        return findByPrimaryKeyOrNull(id)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    SomeVersionedEntity::class.java,
                    mapOf(
                        "id" to id,
                    )
                ),
                SomeVersionedEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(id: DomainId): SomeVersionedEntity? {

        return jdbcOps.queryForList(
            "select * from testing.some_versioned where id = :id",
            SqlParams().apply {
            addValue("id", id)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from testing.some_versioned where id = :id",
            SqlParams().apply {
                addValue("id", id)
           }
        )
       
        return count > 0
       
    }

    fun findOneOrNullBySomeInt(someInt: Int): SomeVersionedEntity? {

        return jdbcOps.queryForList(
            """
            select * from testing.some_versioned
            where some_int = :someInt
            """.trimIndent(),
            SqlParams().apply {
            addValue("someInt", someInt)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    @Throws(EntityNotFoundException::class)
    fun findOneBySomeInt(someInt: Int): SomeVersionedEntity {

        return findOneOrNullBySomeInt(someInt)
            ?: throw EntityNotFoundException("No record with column [some_int = $someInt] found in table testing.some_versioned.", SomeVersionedEntityMeta.TABLE_NAME)

    }


    fun findAllBy(filter: SomeVersionedEntityFilter): List<SomeVersionedEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from testing.some_versioned where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: SomeVersionedEntityFilter): Sequence<DomainId> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id from testing.some_versioned where $whereClause",
            sqlParams,
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<DomainId> {

        return this.jdbcOps.queryForSequence(
            "select id from testing.some_versioned;",
            SqlParams(),
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllBy(filter: SomeVersionedEntityFilter, pageable: Pageable): List<SomeVersionedEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from testing.some_versioned where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<SomeVersionedEntity> {

        return this.jdbcOps.queryForSequence(
            "select * from testing.some_versioned;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun existsBySomeInt(someInt: Int): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*) from testing.some_versioned
            where some_int = :someInt
            """.trimIndent(),
            SqlParams().apply {
            addValue("someInt", someInt)
            }
        )

        return count > 0

    }


    fun upsertBySomeInt(upsertEntity: SomeVersionedEntity): SomeVersionedEntity {

        val persistedEntity = jdbcOps.execute(
            """
            insert into testing.some_versioned (
                c_ts,
                id,
                some_int,
                some_string,
                v
            ) values (
                :createdTimestampUtc,
                :id,
                :someInt,
                :someString,
                :version
            )
            on conflict (some_int)
            do update set
                some_int = :someInt,
                some_string = :someString,
                v = testing.some_versioned.v + 1
            returning *;
            """.trimIndent(),
            SqlParams().apply {
            addValue("createdTimestampUtc", upsertEntity.createdTimestampUtc)
            addValue("id", upsertEntity.id)
            addValue("someInt", upsertEntity.someInt)
            addValue("someString", upsertEntity.someString)
            addValue("version", upsertEntity.version)
            },
            { ps: PreparedStatement ->
                val rs = ps.executeQuery()
                rs.next()
                entityRowMapper.mapRow(ResultSetAdapter(rs))
            }
        )

        return persistedEntity!!

    }


    fun setFields(updaters: List<SomeVersionedEntityUpdater>) {

        updaters.forEach { setFields(it) }

    }


    fun setFields(updater: SomeVersionedEntityUpdater): Int {

        val sql = StringBuilder()
        val sqlParams = SqlParams()

        sql.append("update testing.some_versioned set ")

        val fieldClauses = updater.fields
            .plus(FieldUpdate("v_incremented", "v", updater.version + 1))
            .map { field ->

                addField(field, sqlParams)
                "${field.dbColumnName} = :${field.classFieldName}"

            }.joinToString(", ")

        sql.append(fieldClauses)
        sql.append(" where id = :id")
        sql.append(" and v = :v")

        sqlParams.addValue("id", updater.id)
        sqlParams.addValue("v", updater.version)
        sqlParams.addValue("v_incremented", updater.version + 1)

        return this.jdbcOps.update(sql.toString(), sqlParams)

    }


    private fun addField(field: FieldUpdate, sqlParams: SqlParams) {

        when (field.classFieldName) {
            "someInt" -> sqlParams.addValue("someInt", field.value as Int)
            "someString" -> sqlParams.addValue("someString", field.value as String)
        }

    }


}

