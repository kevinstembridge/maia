
TODO * Manually test the 'Create' page.


TODO * Consolidate all the form component renderers into one.
    TODO * List them all and the reason why they are currently being used.

TODO * Fix the CRUD playwright tests to work with the 'Create' page instead of a dialog.
    => Ask Claude to modify the existing test to take into account the new 'Create' page.

TODO * Generate the routes.

TODO * ??? Create a convenience DSL to all for the construction of a standard entity CRUD stack.


TODO * rv if CrudApiDef.withEntityForm is still needed now that we have EntityEditPageDef instead.
    TODO * Refactor so that EntityEditReactiveFormHtmlRenderer doesn't need a reference to the CrudApiDef.

TODO * EntityDeleteApiDef no longer needs to reference any front end components. This has been moved to EntityDeleteDialogDef.

TODO * On Edit clicked, switch to an EditPage.

TODO * Add a 'Cancel' button to edit pages.

TODO * Switch from 'Create' dialogs to pages


TODO * Define a many-to-many entity that has extra fields on it (e.g. effectiveTimestamps, approvalStatus).
    => LeftToRightManyToManyEntity
        -> with effective timestamps
        -> with an approval status.
    TODO * Test that we can create a new RightManyEntity and associate it with multiple LeftManys. The assumption
            is that it will be a high cardinality relationship with a small number of LeftManys and a large number
             of RightManys. When creating a LeftMany, we should not be able to associate it with multiple RightManys
             because the number will be too high to manage from a UX perspective.
    TODO * When a RightMany is added to a LeftMany, test that the effectiveFrom timestamp gets set automatically.
    TODO * When a RightMany is removed from a LeftMany, test that the effectiveTo timestamp gets set automatically.
    TODO * Test that when we display a RightMany, we only display the associated LeftManys that are effective.

    Design:
    For a complex entity that has many-to-many relationships, a simple edit dialog will not be suitable.
    We should instead create separate pages to Create, View and Edit the entity.
    The joins to the 'other side' entities will be displayed in a list of cards with each card displaying
    the display name of the other side and the fields from the join entity (effective timestamps, approval status, etc).
    Each card will have a Delete icon.
    Above the list will be an 'Add' button that will open a dialog to create a new join entity.



TODO * Mahana clean build

TODO * Many-to-many relationship.
    => Define a many-to-many relationship and generate the full CRUD stack for it.
        The rowMapper of the Left entity should make a direct query to the Right entity's table
        to fetch the RightPkAndNameDtos. This won't be fast enough to service a search request
        from a blotter. For that, we'll have to store blotter rows in ES.
    TODO * Refactor so that CrudApiDefs don't need to have a mutable list of MTM associations.

TODO * Test edit on an entity that has a foreign key.
    TODO * Manual test
TODO * Create a test that tests all the validation constraints on the AllFieldTypesEntity.
    TODO * Test enum constraint
    TODO * Test maxSize, length, etc
    => ValidationBlackBoxTest.
TODO * Maia Showcase
    TODO * Create a Playwright test for the AllFieldTypesEntity Edit feature.
        TODO * The Edit action column should not be visible to an unauthorized user.
    TODO * Test fetchForEdit with a versioned entity.
    TODO * Remove all dependencies and add back in as needed, the goal is for application
            modules to not have to declare a dependency on something that Maia needs.
