// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.CheckForeignKeyReferencesDialogComponentRenderer

import {Component, OnInit, inject} from '@angular/core';
import {MatButtonModule} from '@angular/material/button';
import {MAT_DIALOG_DATA, MatDialog, MatDialogActions, MatDialogContent, MatDialogRef, MatDialogTitle} from '@angular/material/dialog';
import {MatProgressSpinnerModule} from '@angular/material/progress-spinner';
import {ForeignKeyReferenceService} from '@app/gen-components/common/services/foreign-key-reference.service';
import {MessageDetails, MessagePanelComponent} from '@maia/maia-ui';
import {of} from 'rxjs';
import {catchError, tap} from 'rxjs/operators';



@Component({
    imports: [MatDialogTitle, MatDialogContent, MatProgressSpinnerModule, MessagePanelComponent, MatDialogActions, MatButtonModule],
    selector: 'app-right-check-foreign-key-references-dialog',
    templateUrl: './right-check-foreign-key-references-dialog.component.html'
})
export class RightCheckForeignKeyReferencesDialogComponent implements OnInit {

    
    checking: boolean = false;


    messageDetails = new MessageDetails();


    private readonly dialogRef = inject(MatDialogRef<RightCheckForeignKeyReferencesDialogComponent>);


    private readonly dto = inject<any>(MAT_DIALOG_DATA);


    private readonly foreignKeyReferenceService = inject(ForeignKeyReferenceService);



    ngOnInit() {

        this.foreignKeyReferenceService.checkRightForReferences(this.dto.id)
          .pipe(
            tap(() => this.checking = true),
            catchError(err => {
              this.messageDetails.setErrorDetailsFromHttpError(err);
              return of(null);
            }),
            tap(() => this.checking = false)
          ).subscribe(
            res => {
              if (res?.exists) {
                this.messageDetails.setErrorMessage('Foreign key references to entity ' + res.entityKey + ' exist.');
              } else {
                this.dialogRef.close(true);
              }
            }
          );

    }


    onCancel() {
        this.dialogRef.close();
    }


}
