// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.AngularReactiveFormComponentRenderer

import {Component, OnInit, inject, input, output, signal} from '@angular/core';
import {FormControl, FormGroup, FormsModule, ReactiveFormsModule, Validators} from '@angular/forms';
import {MatAutocomplete, MatAutocompleteTrigger, MatOption} from '@angular/material/autocomplete';
import {MatButtonModule} from '@angular/material/button';
import {MatFormFieldModule} from '@angular/material/form-field';
import {MatInputModule} from '@angular/material/input';
import {MatProgressSpinnerModule} from '@angular/material/progress-spinner';
import {Router} from '@angular/router';
import {AlphaWithHistoryTypeaheadV1EsDoc} from '@app/gen-components/org/maiaframework/showcase/join/AlphaWithHistoryTypeaheadV1EsDoc';
import {BravoWithHistoryFetchForEditDto} from '@app/gen-components/org/maiaframework/showcase/join/BravoWithHistoryFetchForEditDto';
import {BravoWithHistoryUpdateRequestDto} from '@app/gen-components/org/maiaframework/showcase/join/BravoWithHistoryUpdateRequestDto';
import {bravoWithHistory_alphaRequiredValidator} from '@app/gen-components/org/maiaframework/showcase/join/BravoWithHistory_alphaRequiredValidator';
import {AlphaWithHistoryTypeaheadApiService} from '@app/gen-components/org/maiaframework/showcase/join/alpha-with-history-typeahead-api.service';
import {BravoWithHistoryCrudService} from '@app/gen-components/org/maiaframework/showcase/join/bravo-with-history-crud-service';
import {ProblemDetail} from '@maia/maia-ui';
import {of} from 'rxjs';
import {catchError, debounceTime, filter, map, switchMap, tap} from 'rxjs/operators';



@Component({
    imports: [
        FormsModule,
        MatAutocomplete,
        MatAutocompleteTrigger,
        MatButtonModule,
        MatFormFieldModule,
        MatInputModule,
        MatOption,
        MatProgressSpinnerModule,
        ReactiveFormsModule,
    ],
    providers: [
        AlphaWithHistoryTypeaheadApiService,
    ],
    selector: 'app-bravo-with-history-entity-edit-form',
    styleUrls: ['./bravo-with-history-entity-edit-form.scss'],
    templateUrl: './bravo-with-history-entity-edit-form.html'
})
export class BravoWithHistoryEntityEditForm implements OnInit {


    entityId = input.required<string>();


    onCancel = output();


    private readonly formService = inject(BravoWithHistoryCrudService);


    problemDetail = signal<ProblemDetail | null>(null);


    filteredAlphaWithHistory: AlphaWithHistoryTypeaheadV1EsDoc[] = [];


    filteredAlphaWithHistoryIsLoading = signal(false);



    private readonly alphaWithHistoryTypeaheadApiService = inject(AlphaWithHistoryTypeaheadApiService);


    formGroup: FormGroup;


    loading = signal(true);


    private readonly router = inject(Router);


    constructor() {

        this.formGroup = new FormGroup(
            {
                someInt: new FormControl(0, { updateOn: 'change' }),
                someString: new FormControl('', { updateOn: 'change', validators: [Validators.required, Validators.maxLength(100)] }),
                id: new FormControl({value: '', disabled: true}),
                alpha: new FormControl('', { updateOn: 'change', validators: [bravoWithHistory_alphaRequiredValidator()] }),
                version: new FormControl({value: 0, disabled: true}),
            },
        );

    }


    ngOnInit() {

        this.formGroup.controls['alphaWithHistory'].valueChanges
            .pipe(
                debounceTime(300),
                filter(value => typeof value === 'string'),
                tap(() => {
                    this.filteredAlphaWithHistory = [];
                    this.filteredAlphaWithHistoryIsLoading.set(true);
                }),
                switchMap(value => this.alphaWithHistoryTypeaheadApiService.search(value)
                    .pipe(
                        catchError(err => {
                            this.filteredAlphaWithHistoryIsLoading.set(false);
                            console.error(err);
                            return of([]);
                        })
                    )
                ),
                tap(() => this.filteredAlphaWithHistoryIsLoading.set(false))
            ).subscribe(res => {
                this.filteredAlphaWithHistory = res;
            });

        this.formService.fetchForEdit(this.entityId()).subscribe({
            next: (dto: BravoWithHistoryFetchForEditDto) => {
                this.formGroup.patchValue({
                    alpha: dto.alpha,
                    id: dto.id,
                    someInt: dto.someInt,
                    someString: dto.someString,
                    version: dto.version,
                });
                this.loading.set(false);
            },
            error: (err) => {
                this.problemDetail.set(err.error);
                this.loading.set(false);
            }
        });

    }


    alphaWithHistoryResultFormatter = (result: any) => result.someString;


    onSubmit() {

        this.problemDetail.set(null);

        if (this.formGroup.invalid) {
            return;
        }

        const requestDto = {
            alpha: this.formGroup.getRawValue().alphaWithHistory.id,
            id: this.formGroup.getRawValue().id,
            someInt: this.formGroup.getRawValue().someInt,
            someString: this.formGroup.getRawValue().someString,
            version: this.formGroup.getRawValue().version,
        } as BravoWithHistoryUpdateRequestDto;

        this.formService.edit(requestDto).subscribe({
            next: () => {
                this.router.navigate(['/bravo-with-history/view', this.entityId()]);
            },
            error: err => {
                this.problemDetail.set(err.error);
            }
        });

    }


    onCancelClicked(): void {
        this.onCancel.emit();
    }


}

