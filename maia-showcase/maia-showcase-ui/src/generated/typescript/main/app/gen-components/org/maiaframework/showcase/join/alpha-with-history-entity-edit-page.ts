// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.EntityEditFormPageComponentRenderer

import {ChangeDetectionStrategy, Component, inject} from '@angular/core';
import {toSignal} from '@angular/core/rxjs-interop';
import {ActivatedRoute, Router} from '@angular/router';
import {AlphaWithHistoryEntityEditForm} from '@app/gen-components/org/maiaframework/showcase/join/alpha-with-history-entity-edit-form';
import {PageLayout} from '@maia/maia-ui';
import {map} from 'rxjs';



@Component({
    changeDetection: ChangeDetectionStrategy.OnPush,
    imports: [
        PageLayout,
        AlphaWithHistoryEntityEditForm
    ],
    selector: 'app-alpha-with-history-entity-edit-page',
    templateUrl: './alpha-with-history-entity-edit-page.html'
})
export class AlphaWithHistoryEntityEditPage {


    private readonly route = inject(ActivatedRoute);


    private readonly router = inject(Router);


    protected readonly entityId = toSignal(
        this.route.paramMap.pipe(
            map(p => p.get('id'))
        )
    );


    onSaveClicked(): void {
        const id = this.entityId();
        if (id) {
            this.router.navigate(['/alpha-with-history/view', id]);
        }
    }


    onCancelClicked(): void {
        const id = this.entityId();
        if (id) {
            this.router.navigate(['/alpha-with-history/view', id]);
        }
    }


}

