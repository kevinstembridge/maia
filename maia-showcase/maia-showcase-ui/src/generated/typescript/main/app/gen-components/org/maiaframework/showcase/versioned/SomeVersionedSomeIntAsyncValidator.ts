// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.AsyncValidatorRenderer

import {Injectable, inject} from '@angular/core';
import {AbstractControl, AsyncValidator, FormControl, FormGroup, ValidationErrors} from '@angular/forms';
import {SomeVersionedSomeIntRequestDto} from '@app/gen-components/org/maiaframework/showcase/versioned/SomeVersionedSomeIntRequestDto';
import {SomeVersionedCrudService} from '@app/gen-components/org/maiaframework/showcase/versioned/some-versioned-crud.service';
import {Observable, of} from 'rxjs';
import {catchError, map} from 'rxjs/operators';



@Injectable({
    providedIn: 'root'
})
export class SomeVersionedSomeIntAsyncValidator implements AsyncValidator {


    private readonly apiService = inject(SomeVersionedCrudService);


    validate(ctrl: AbstractControl): Observable<ValidationErrors | null> {

        if (ctrl.value === false || ctrl.dirty === false) {
            return of(null);
        }

        const requestBody = {
            someInt: ctrl.value
        } as SomeVersionedSomeIntRequestDto;

        return this.apiService.existsBySomeInt(requestBody).pipe(
            map(response => (response.invalid ? { message: response.message, notUnique: true } : null)),
            catchError(() => of(null))
        );

    }


}

