// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.EntityReactiveFormComponentRenderer

import {Component, OnInit, inject, output, signal} from '@angular/core';
import {FormControl, FormGroup, FormsModule, ReactiveFormsModule, Validators} from '@angular/forms';
import {MatAutocompleteModule} from '@angular/material/autocomplete';
import {MatButtonModule} from '@angular/material/button';
import {MatOptionModule} from '@angular/material/core';
import {MAT_DIALOG_DATA, MatDialog, MatDialogActions, MatDialogContent, MatDialogRef, MatDialogTitle} from '@angular/material/dialog';
import {MatFormFieldModule} from '@angular/material/form-field';
import {MatInputModule} from '@angular/material/input';
import {MatProgressSpinnerModule} from '@angular/material/progress-spinner';
import {UserGroupMembershipFetchForEditDto} from '@app/gen-components/org/maiaframework/showcase/user/UserGroupMembershipFetchForEditDto';
import {UserGroupMembershipUpdateRequestDto} from '@app/gen-components/org/maiaframework/showcase/user/UserGroupMembershipUpdateRequestDto';
import {UserGroupMembershipCrudService} from '@app/gen-components/org/maiaframework/showcase/user/user-group-membership-crud.service';
import {ProblemDetail} from '@maia/maia-ui';
import {Observable, Subject, of} from 'rxjs';
import {catchError, debounceTime, distinctUntilChanged, filter, map, switchMap, tap} from 'rxjs/operators';



@Component({
    imports: [
        FormsModule,
        MatAutocompleteModule,
        MatButtonModule,
        MatDialogActions,
        MatDialogContent,
        MatDialogTitle,
        MatFormFieldModule,
        MatInputModule,
        MatOptionModule,
        MatProgressSpinnerModule,
        ReactiveFormsModule,
    ],
    selector: 'app-user-group-membership-edit-dialog',
    styleUrls: ['./user-group-membership-edit-dialog.component.scss'],
    templateUrl: './user-group-membership-edit-dialog.component.html'
})
export class UserGroupMembershipEditDialogComponent implements OnInit {


    problemDetail = signal<ProblemDetail | null>(null);


    loading = signal(true);


    formGroup: FormGroup;


    readonly dialogRef = inject(MatDialogRef<UserGroupMembershipEditDialogComponent>);


    private readonly formService = inject(UserGroupMembershipCrudService);


    private readonly entityId = inject<string>(MAT_DIALOG_DATA);


    constructor() {

        this.formGroup = new FormGroup(
            {
                effectiveFrom: new FormControl({value: '', disabled: true}),
                effectiveTo: new FormControl({value: '', disabled: true}),
                id: new FormControl({value: '', disabled: true}),
                userGroup: new FormControl('', { updateOn: 'change' }),
                user: new FormControl('', { updateOn: 'change' }),
                version: new FormControl({value: 0, disabled: true}),
            },
        );

    }


    ngOnInit() {

        this.formService.fetchForEdit(this.entityId).subscribe({
            next: (dto: UserGroupMembershipFetchForEditDto) => {
                this.formGroup.patchValue({
                    effectiveFrom: dto.effectiveFrom,
                    effectiveTo: dto.effectiveTo,
                    id: dto.id,
                    version: dto.version,
                });
                this.loading.set(false);
            },
            error: (err) => {
                this.problemDetail.set(err.error);
                this.loading.set(false);
            }
        });

    }


    onSubmit() {

        this.problemDetail.set(null);

        if (this.formGroup.invalid) {
            return;
        }

        const requestDto = {
            id: this.formGroup.getRawValue().id,
            user: this.formGroup.getRawValue().user,
            userGroup: this.formGroup.getRawValue().userGroup,
            version: this.formGroup.getRawValue().version,
        } as UserGroupMembershipUpdateRequestDto;

        this.formService.edit(requestDto)
            .subscribe({
                next: () => {
                    this.dialogRef.close(true);
                },
                error: err => {
                    this.problemDetail.set(err.error);
                }
            });

    }


    onCancel(): void {
        this.dialogRef.close();
    }


}

