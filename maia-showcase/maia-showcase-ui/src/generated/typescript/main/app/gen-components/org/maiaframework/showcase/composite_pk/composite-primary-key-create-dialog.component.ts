// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.EntityReactiveFormComponentRenderer

import {Component, OnInit, inject, output, signal} from '@angular/core';
import {FormControl, FormGroup, FormsModule, ReactiveFormsModule, Validators} from '@angular/forms';
import {MatAutocompleteModule} from '@angular/material/autocomplete';
import {MatButtonModule} from '@angular/material/button';
import {MatOptionModule} from '@angular/material/core';
import {MAT_DIALOG_DATA, MatDialog, MatDialogActions, MatDialogContent, MatDialogRef, MatDialogTitle} from '@angular/material/dialog';
import {MatFormFieldModule} from '@angular/material/form-field';
import {MatInputModule} from '@angular/material/input';
import {CompositePrimaryKeyCreateRequestDto} from '@app/gen-components/org/maiaframework/showcase/composite_pk/CompositePrimaryKeyCreateRequestDto';
import {CompositePrimaryKeyCrudService} from '@app/gen-components/org/maiaframework/showcase/composite_pk/composite-primary-key-crud.service';
import {ProblemDetail} from '@maia/maia-ui';
import {Observable, Subject, of} from 'rxjs';
import {catchError, debounceTime, distinctUntilChanged, filter, map, switchMap, tap} from 'rxjs/operators';



@Component({
    imports: [
        FormsModule,
        MatAutocompleteModule,
        MatButtonModule,
        MatDialogActions,
        MatDialogContent,
        MatDialogTitle,
        MatFormFieldModule,
        MatInputModule,
        MatOptionModule,
        ReactiveFormsModule,
    ],
    selector: 'app-composite-primary-key-create-dialog',
    styleUrls: ['./composite-primary-key-create-dialog.component.scss'],
    templateUrl: './composite-primary-key-create-dialog.component.html'
})
export class CompositePrimaryKeyCreateDialogComponent implements OnInit {


    problemDetail = signal<ProblemDetail | null>(null);


    formGroup: FormGroup;


    readonly dialogRef = inject(MatDialogRef<CompositePrimaryKeyCreateDialogComponent>);


    private readonly formService = inject(CompositePrimaryKeyCrudService);


    constructor() {

        this.formGroup = new FormGroup(
            {
                someString: new FormControl('', { updateOn: 'change', validators: [Validators.required, Validators.maxLength(100)] }),
                someInt: new FormControl(0, { updateOn: 'change' }),
                someModifiableString: new FormControl('', { updateOn: 'change', validators: [Validators.required, Validators.maxLength(100)] }),
            },
        );

    }


    ngOnInit() {

    }


    onSubmit() {

        this.problemDetail.set(null);

        if (this.formGroup.invalid) {
            return;
        }

        const requestDto = {
            someInt: this.formGroup.getRawValue().someInt,
            someModifiableString: this.formGroup.getRawValue().someModifiableString,
            someString: this.formGroup.getRawValue().someString,
        } as CompositePrimaryKeyCreateRequestDto;

        this.formService.create(requestDto)
            .subscribe({
                next: () => {
                    this.dialogRef.close(true);
                },
                error: err => {
                    this.problemDetail.set(err.error);
                }
            });

    }


    onCancel(): void {
        this.dialogRef.close();
    }


}

