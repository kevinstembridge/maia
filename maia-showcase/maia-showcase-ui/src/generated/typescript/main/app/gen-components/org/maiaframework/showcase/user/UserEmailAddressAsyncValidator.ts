// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.AsyncValidatorRenderer

import {Injectable, inject} from '@angular/core';
import {AbstractControl, AsyncValidator, FormControl, FormGroup, ValidationErrors} from '@angular/forms';
import {UserEmailAddressRequestDto} from '@app/gen-components/org/maiaframework/showcase/user/UserEmailAddressRequestDto';
import {UserCrudService} from '@app/gen-components/org/maiaframework/showcase/user/user-crud.service';
import {Observable, of} from 'rxjs';
import {catchError, map} from 'rxjs/operators';



@Injectable({
    providedIn: 'root'
})
export class UserEmailAddressAsyncValidator implements AsyncValidator {


    private readonly apiService = inject(UserCrudService);


    validate(ctrl: AbstractControl): Observable<ValidationErrors | null> {

        if (ctrl.value === false || ctrl.dirty === false) {
            return of(null);
        }

        const requestBody = {
            emailAddress: ctrl.value
        } as UserEmailAddressRequestDto;

        return this.apiService.existsByEmailAddress(requestBody).pipe(
            map(response => (response.invalid ? { message: response.message, notUnique: true } : null)),
            catchError(() => of(null))
        );

    }


}

