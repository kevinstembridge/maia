// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.CurrentUserStoreRenderer

import {computed} from '@angular/core';
import {UserSummaryDto} from '@app/gen-components/org/maiaframework/showcase/auth/UserSummaryDto';
import {patchState, signalStore, withComputed, withMethods, withState} from '@ngrx/signals';



type CurrentUserState = {
    currentUser: UserSummaryDto | null;
};


const initialState: CurrentUserState = {
    currentUser: null
};


export const CurrentUserStore = signalStore(


    { providedIn: 'root'},


    withState(initialState),


    withComputed(({ currentUser }) => ({

        isSignedIn: computed<boolean>(() => {
            return !!currentUser();
        }),

    })),


    withMethods((store) => ({

        setCurrentUser(user: UserSummaryDto | null): void {

            patchState(store, { currentUser: user });

        },

    })),


);
