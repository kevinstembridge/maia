// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.AngularReactiveFormComponentRenderer

import {Component, OnInit, inject, input, output, signal} from '@angular/core';
import {FormControl, FormGroup, FormsModule, ReactiveFormsModule, Validators} from '@angular/forms';
import {MatButtonModule} from '@angular/material/button';
import {MatFormFieldModule} from '@angular/material/form-field';
import {MatInputModule} from '@angular/material/input';
import {MatProgressSpinnerModule} from '@angular/material/progress-spinner';
import {Router} from '@angular/router';
import {LeftToRightComplexFetchForEditDto} from '@app/gen-components/org/maiaframework/showcase/many-to-many/LeftToRightComplexFetchForEditDto';
import {LeftToRightComplexUpdateRequestDto} from '@app/gen-components/org/maiaframework/showcase/many-to-many/LeftToRightComplexUpdateRequestDto';
import {LeftToRightComplexCrudService} from '@app/gen-components/org/maiaframework/showcase/many-to-many/left-to-right-complex-crud-service';
import {ProblemDetail} from '@maia/maia-ui';



@Component({
    imports: [
        FormsModule,
        MatButtonModule,
        MatFormFieldModule,
        MatInputModule,
        MatProgressSpinnerModule,
        ReactiveFormsModule,
    ],
    selector: 'app-left-to-right-complex-entity-edit-form',
    styleUrls: ['./left-to-right-complex-entity-edit-form.scss'],
    templateUrl: './left-to-right-complex-entity-edit-form.html'
})
export class LeftToRightComplexEntityEditForm implements OnInit {


    entityId = input.required<string>();


    onCancel = output();


    private readonly formService = inject(LeftToRightComplexCrudService);


    problemDetail = signal<ProblemDetail | null>(null);


    formGroup: FormGroup;


    loading = signal(true);


    private readonly router = inject(Router);


    constructor() {

        this.formGroup = new FormGroup(
            {
                effectiveFrom: new FormControl({value: '', disabled: true}),
                effectiveTo: new FormControl({value: '', disabled: true}),
                someIntOnComplex: new FormControl(0, { updateOn: 'change' }),
                id: new FormControl({value: '', disabled: true}),
                left: new FormControl('', { updateOn: 'change' }),
                right: new FormControl('', { updateOn: 'change' }),
            },
        );

    }


    ngOnInit() {

        this.formService.fetchForEdit(this.entityId()).subscribe({
            next: (dto: LeftToRightComplexFetchForEditDto) => {
                this.formGroup.patchValue({
                    effectiveFrom: dto.effectiveFrom,
                    effectiveTo: dto.effectiveTo,
                    id: dto.id,
                    someIntOnComplex: dto.someIntOnComplex,
                });
                this.loading.set(false);
            },
            error: (err) => {
                this.problemDetail.set(err.error);
                this.loading.set(false);
            }
        });

    }


    onSubmit() {

        this.problemDetail.set(null);

        if (this.formGroup.invalid) {
            return;
        }

        const requestDto = {
            id: this.formGroup.getRawValue().id,
            left: this.formGroup.getRawValue().left,
            right: this.formGroup.getRawValue().right,
            someIntOnComplex: this.formGroup.getRawValue().someIntOnComplex,
        } as LeftToRightComplexUpdateRequestDto;

        this.formService.edit(requestDto).subscribe({
            next: () => {
                this.router.navigate(['/left-to-right-complex/view', this.entityId()]);
            },
            error: err => {
                this.problemDetail.set(err.error);
            }
        });

    }


    onCancelClicked(): void {
        this.onCancel.emit();
    }


}

