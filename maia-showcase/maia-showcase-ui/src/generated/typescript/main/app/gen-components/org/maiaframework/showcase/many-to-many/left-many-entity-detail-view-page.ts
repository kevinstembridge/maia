// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.EntityDetailViewPageComponentRenderer

import {ChangeDetectionStrategy, Component, inject} from '@angular/core';
import {toSignal} from '@angular/core/rxjs-interop';
import {MatButtonModule} from '@angular/material/button';
import {MatIconModule} from '@angular/material/icon';
import {ActivatedRoute, Router} from '@angular/router';
import {PageLayout} from '@app/components/page-layout/page-layout';
import {Authority} from '@app/gen-components/org/maiaframework/showcase/Authority';
import {AuthService} from '@app/gen-components/org/maiaframework/showcase/auth-service';
import {LeftManyEntityDetailView} from '@app/gen-components/org/maiaframework/showcase/many-to-many/left-many-entity-detail-view';
import {map} from 'rxjs';


@Component({
    changeDetection: ChangeDetectionStrategy.OnPush,
    imports: [
        MatButtonModule,
        MatIconModule,
        PageLayout,
        LeftManyEntityDetailView
    ],
    selector: 'app-left-many-entity-detail-view-page',
    templateUrl: './left-many-entity-detail-view-page.html'
})
export class LeftManyEntityDetailViewPage {


    private readonly route = inject(ActivatedRoute);


    private readonly router = inject(Router);


    private readonly authService = inject(AuthService);


    protected readonly entityId = toSignal(
        this.route.paramMap.pipe(
            map(p => p.get('id'))
        )
    );


    protected get canEdit(): boolean {
        return this.authService.currentUserHasThisAuthority(Authority.WRITE);
    }


    onEditClicked(): void {
        const id = this.entityId();
        if (id) {
            this.router.navigate(['/left-many/edit', id]);
        }
    }


    onBlotterClicked(): void {
        this.router.navigate(['/left-many-blotter']);
    }


}

