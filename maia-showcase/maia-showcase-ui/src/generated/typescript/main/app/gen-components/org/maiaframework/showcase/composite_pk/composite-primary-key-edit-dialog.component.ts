// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.ui.EntityReactiveFormComponentRenderer

import {Component, OnInit, inject, output, signal} from '@angular/core';
import {FormControl, FormGroup, FormsModule, ReactiveFormsModule, Validators} from '@angular/forms';
import {MatAutocompleteModule} from '@angular/material/autocomplete';
import {MatButtonModule} from '@angular/material/button';
import {MatOptionModule} from '@angular/material/core';
import {MAT_DIALOG_DATA, MatDialog, MatDialogActions, MatDialogContent, MatDialogRef, MatDialogTitle} from '@angular/material/dialog';
import {MatFormFieldModule} from '@angular/material/form-field';
import {MatInputModule} from '@angular/material/input';
import {MatProgressSpinnerModule} from '@angular/material/progress-spinner';
import {CompositePrimaryKeyFetchForEditDto} from '@app/gen-components/org/maiaframework/showcase/composite_pk/CompositePrimaryKeyFetchForEditDto';
import {CompositePrimaryKeyUpdateRequestDto} from '@app/gen-components/org/maiaframework/showcase/composite_pk/CompositePrimaryKeyUpdateRequestDto';
import {CompositePrimaryKeyCrudService} from '@app/gen-components/org/maiaframework/showcase/composite_pk/composite-primary-key-crud.service';
import {ProblemDetail} from '@maia/maia-ui';
import {Observable, Subject, of} from 'rxjs';
import {catchError, debounceTime, distinctUntilChanged, filter, map, switchMap, tap} from 'rxjs/operators';



@Component({
    imports: [
        FormsModule,
        MatAutocompleteModule,
        MatButtonModule,
        MatDialogActions,
        MatDialogContent,
        MatDialogTitle,
        MatFormFieldModule,
        MatInputModule,
        MatOptionModule,
        MatProgressSpinnerModule,
        ReactiveFormsModule,
    ],
    selector: 'app-composite-primary-key-edit-dialog',
    styleUrls: ['./composite-primary-key-edit-dialog.component.scss'],
    templateUrl: './composite-primary-key-edit-dialog.component.html'
})
export class CompositePrimaryKeyEditDialogComponent implements OnInit {


    problemDetail = signal<ProblemDetail | null>(null);


    loading = signal(true);


    formGroup: FormGroup;


    readonly dialogRef = inject(MatDialogRef<CompositePrimaryKeyEditDialogComponent>);


    private readonly formService = inject(CompositePrimaryKeyCrudService);


    private readonly entityId = inject<{someString: string, someInt: number}>(MAT_DIALOG_DATA);


    constructor() {

        this.formGroup = new FormGroup(
            {
                someString: new FormControl({value: '', disabled: true}),
                someInt: new FormControl({value: 0, disabled: true}),
                someModifiableString: new FormControl('', { updateOn: 'change', validators: [Validators.required, Validators.maxLength(100)] }),
                version: new FormControl({value: 0, disabled: true}),
            },
        );

    }


    ngOnInit() {

        this.formService.fetchForEdit(this.entityId).subscribe({
            next: (dto: CompositePrimaryKeyFetchForEditDto) => {
                this.formGroup.patchValue({
                    someString: dto.someString,
                    someInt: dto.someInt,
                    someModifiableString: dto.someModifiableString,
                    version: dto.version,
                });
                this.loading.set(false);
            },
            error: (err) => {
                this.problemDetail.set(err.error);
                this.loading.set(false);
            }
        });

    }


    onSubmit() {

        this.problemDetail.set(null);

        if (this.formGroup.invalid) {
            return;
        }

        const requestDto = {
            someInt: this.formGroup.getRawValue().someInt,
            someModifiableString: this.formGroup.getRawValue().someModifiableString,
            someString: this.formGroup.getRawValue().someString,
            version: this.formGroup.getRawValue().version,
        } as CompositePrimaryKeyUpdateRequestDto;

        this.formService.edit(requestDto)
            .subscribe({
                next: () => {
                    this.dialogRef.close(true);
                },
                error: err => {
                    this.problemDetail.set(err.error);
                }
            });

    }


    onCancel(): void {
        this.dialogRef.close();
    }


}

