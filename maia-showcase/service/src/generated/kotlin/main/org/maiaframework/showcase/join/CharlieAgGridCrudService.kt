// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.CrudServiceRenderer

package org.maiaframework.showcase.join

import org.maiaframework.domain.DomainId
import org.maiaframework.problem.MaiaProblems
import org.maiaframework.webapp.domain.auth.CurrentUserHolder
import org.slf4j.LoggerFactory
import org.springframework.stereotype.Component
import java.time.Instant


@Component
class CharlieAgGridCrudService(
    private val charlieAgGridCrudNotifier: CharlieAgGridCrudNotifier,
    private val entityRepo: CharlieAgGridRepo,
    private val maiaProblems: MaiaProblems
) {


    private val logger = LoggerFactory.getLogger(CharlieAgGridCrudService::class.java)


    fun create(createDto: CharlieAgGridCreateRequestDto): CharlieAgGridEntity {

        logger.info("BEGIN: create CharlieAgGrid. dto=$createDto")

        val entity: CharlieAgGridEntity = buildEntity(createDto)

        return create(entity)

    }


    private fun buildEntity(createDto: CharlieAgGridCreateRequestDto): CharlieAgGridEntity {

        val bravo: DomainId = createDto.bravo
        val someInt: Int = createDto.someInt
        val someString: String = createDto.someString
        val id = DomainId.newId()
        val createdTimestampUtc = Instant.now()

        return CharlieAgGridEntity(
            bravo,
            createdTimestampUtc,
            id,
            someInt,
            someString
        )

    }


    fun create(entity: CharlieAgGridEntity): CharlieAgGridEntity {

        this.entityRepo.insert(entity)
        this.charlieAgGridCrudNotifier.onEntityCreated(entity)
        return entity

    }


    fun fetchForEdit(id: DomainId): CharlieAgGridFetchForEditDto {

        return this.entityRepo.fetchForEdit(id)

    }


    fun update(editDto: CharlieAgGridUpdateRequestDto) {

        val id = editDto.id
        val updater = CharlieAgGridEntityUpdater.forPrimaryKey(id) {
            bravo(editDto.bravo)
        }

        setFields(updater)

    }


    fun updateBravo(editDto: CharlieAgGridUpdate_bravoRequestDto) {

        val currentUsername = CurrentUserHolder.currentUsername

        logger.info("BEGIN: updateBravo. currentUsername=${currentUsername}, dto=$editDto")

        val updater = CharlieAgGridEntityUpdater.forPrimaryKey(editDto.id) {
            bravo(editDto.bravo)
        }

        setFields(updater)

    }


    fun setFields(updater: CharlieAgGridEntityUpdater): Int {
        
        val count = this.entityRepo.setFields(updater)
        this.charlieAgGridCrudNotifier.onEntityUpdated(updater.id)
        return count
        
    }


    fun delete(id: DomainId) {

        val entityToDelete = this.entityRepo.findByPrimaryKeyOrNull(id)
                ?: return

        this.entityRepo.deleteByPrimaryKey(id)
        this.charlieAgGridCrudNotifier.onEntityDeleted(entityToDelete)

    }


}

