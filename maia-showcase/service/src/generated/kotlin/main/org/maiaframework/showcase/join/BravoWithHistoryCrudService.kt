// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.CrudServiceRenderer

package org.maiaframework.showcase.join

import org.maiaframework.domain.DomainId
import org.maiaframework.problem.MaiaProblems
import org.maiaframework.webapp.domain.auth.CurrentUserHolder
import org.slf4j.LoggerFactory
import org.springframework.security.access.prepost.PreAuthorize
import org.springframework.stereotype.Component
import org.springframework.transaction.annotation.Transactional
import java.time.Instant


@Component
class BravoWithHistoryCrudService(
    private val bravoWithHistoryCrudNotifier: BravoWithHistoryCrudNotifier,
    private val entityRepo: BravoWithHistoryRepo,
    private val maiaProblems: MaiaProblems
) {


    private val logger = LoggerFactory.getLogger(BravoWithHistoryCrudService::class.java)


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun create(createDto: BravoWithHistoryCreateRequestDto): BravoWithHistoryEntity {

        logger.info("BEGIN: create BravoWithHistory. dto=$createDto")

        val entity: BravoWithHistoryEntity = buildEntity(createDto)

        return create(entity)

    }


    private fun buildEntity(createDto: BravoWithHistoryCreateRequestDto): BravoWithHistoryEntity {

        val alpha: DomainId = createDto.alpha
        val someInt: Int = createDto.someInt
        val someString: String = createDto.someString
        val id = DomainId.newId()
        val createdTimestamp = Instant.now()
        val version = 1L

        return BravoWithHistoryEntity(
            alpha,
            createdTimestamp,
            id,
            someInt,
            someString,
            version
        )

    }


    @Transactional
    fun create(entity: BravoWithHistoryEntity): BravoWithHistoryEntity {

        this.entityRepo.insert(entity)
        this.bravoWithHistoryCrudNotifier.onEntityCreated(entity)
        return entity

    }


    @Transactional(readOnly = true)
    fun fetchForEdit(id: DomainId): BravoWithHistoryFetchForEditDto {

        return this.entityRepo.fetchForEdit(id)

    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun update(editDto: BravoWithHistoryUpdateRequestDto) {

        val id = editDto.id
        val version = editDto.version
        val updater = BravoWithHistoryEntityUpdater.forPrimaryKey(id, version) {
            alpha(editDto.alpha)
            someInt(editDto.someInt)
            someString(editDto.someString)
        }

        setFields(updater)

    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun updateSomeInt(editDto: BravoWithHistoryUpdate_someIntRequestDto) {

        val currentUsername = CurrentUserHolder.currentUsername

        logger.info("BEGIN: updateSomeInt. currentUsername=${currentUsername}, dto=$editDto")

        val version = editDto.version

        val updater = BravoWithHistoryEntityUpdater.forPrimaryKey(editDto.id, version) {
            someInt(editDto.someInt)
        }

        setFields(updater)

    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun updateSomeString(editDto: BravoWithHistoryUpdate_someStringRequestDto) {

        val currentUsername = CurrentUserHolder.currentUsername

        logger.info("BEGIN: updateSomeString. currentUsername=${currentUsername}, dto=$editDto")

        val version = editDto.version

        val updater = BravoWithHistoryEntityUpdater.forPrimaryKey(editDto.id, version) {
            someString(editDto.someString)
        }

        setFields(updater)

    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun updateAlpha(editDto: BravoWithHistoryUpdate_alphaRequestDto) {

        val currentUsername = CurrentUserHolder.currentUsername

        logger.info("BEGIN: updateAlpha. currentUsername=${currentUsername}, dto=$editDto")

        val version = editDto.version

        val updater = BravoWithHistoryEntityUpdater.forPrimaryKey(editDto.id, version) {
            alpha(editDto.alpha)
        }

        setFields(updater)

    }


    @Transactional
    fun setFields(updater: BravoWithHistoryEntityUpdater): Int {

        val count = this.entityRepo.setFields(updater)
        this.bravoWithHistoryCrudNotifier.onEntityUpdated(updater.id)
        return count
        
    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun delete(id: DomainId) {

        val entityToDelete = this.entityRepo.findByPrimaryKeyOrNull(id)
                ?: return

        this.entityRepo.deleteByPrimaryKey(id)
        this.bravoWithHistoryCrudNotifier.onEntityDeleted(entityToDelete)

    }


}

