// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.TypeaheadServiceRenderer

package org.maiaframework.showcase.join

import co.elastic.clients.elasticsearch.ElasticsearchClient
import co.elastic.clients.elasticsearch._types.query_dsl.TextQueryType
import co.elastic.clients.elasticsearch.core.SearchRequest
import co.elastic.clients.elasticsearch.core.SearchResponse
import org.maiaframework.elasticsearch.SearchTerm
import org.maiaframework.props.Props
import org.springframework.stereotype.Component


@Component
class AlphaTypeaheadService(
    private val elasticClient: ElasticsearchClient,
    private val esIndex: AlphaTypeaheadEsIndex,
    private val props: Props
) {


    fun search(searchTerm: SearchTerm): List<AlphaTypeaheadV1EsDoc> {

        val searchRequest = searchRequest(searchTerm)
        val searchResponse = this.elasticClient.search(searchRequest, AlphaTypeaheadV1EsDoc::class.java)
        val searchResults = buildResults(searchResponse)
        return searchResults

    }


    private fun searchRequest(searchTerm: SearchTerm): SearchRequest {

        return SearchRequest.of { r ->
            r.index(this.esIndex.indexName().asString)
                .query { q ->
                    q.multiMatch { m ->
                        m.query(searchTerm.value)
                            .fields(
                                "someString",
                                "someString._2gram",
                                "someString._3gram"
                            ).type(TextQueryType.BoolPrefix)
                    }
                }.size(searchSize())
        }

    }


    private fun searchSize(): Int {

       return this.props.getIntOrNull("typeahead.search_size.org.maiaframework.showcase.join.AlphaTypeaheadV1EsDoc") ?: 40

   }


    private fun buildResults(searchResponse: SearchResponse<AlphaTypeaheadV1EsDoc>): List<AlphaTypeaheadV1EsDoc> {

        return searchResponse.hits()
                .hits()
                .mapNotNull { it.source() }
                .sortedBy { it.someString.toString() }

    }


}

