// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.CrudServiceRenderer

package org.maiaframework.showcase.join

import org.maiaframework.domain.DomainId
import org.maiaframework.problem.MaiaProblems
import org.maiaframework.webapp.domain.auth.CurrentUserHolder
import org.slf4j.LoggerFactory
import org.springframework.security.access.prepost.PreAuthorize
import org.springframework.stereotype.Component
import org.springframework.transaction.annotation.Transactional
import java.time.Instant


@Component
class AlphaWithHistoryCrudService(
    private val alphaWithHistoryCrudNotifier: AlphaWithHistoryCrudNotifier,
    private val bravoWithHistoryHistoryRepo: BravoWithHistoryHistoryRepo,
    private val bravoWithHistoryRepo: BravoWithHistoryRepo,
    private val entityRepo: AlphaWithHistoryRepo,
    private val maiaProblems: MaiaProblems
) {


    private val logger = LoggerFactory.getLogger(AlphaWithHistoryCrudService::class.java)


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun create(createDto: AlphaWithHistoryCreateRequestDto): AlphaWithHistoryEntity {

        logger.info("BEGIN: create AlphaWithHistory. dto=$createDto")

        val entity: AlphaWithHistoryEntity = buildEntity(createDto)

        return create(entity)

    }


    private fun buildEntity(createDto: AlphaWithHistoryCreateRequestDto): AlphaWithHistoryEntity {

        val someInt: Int = createDto.someInt
        val someString: String = createDto.someString
        val id = DomainId.newId()
        val createdTimestamp = Instant.now()
        val version = 1L

        return AlphaWithHistoryEntity(
            createdTimestamp,
            id,
            someInt,
            someString,
            version
        )

    }


    @Transactional
    fun create(entity: AlphaWithHistoryEntity): AlphaWithHistoryEntity {

        this.entityRepo.insert(entity)
        this.alphaWithHistoryCrudNotifier.onEntityCreated(entity)
        return entity

    }


    @Transactional(readOnly = true)
    fun fetchForEdit(id: DomainId): AlphaWithHistoryFetchForEditDto {

        return this.entityRepo.fetchForEdit(id)

    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun update(editDto: AlphaWithHistoryUpdateRequestDto) {

        val id = editDto.id
        val version = editDto.version
        val updater = AlphaWithHistoryEntityUpdater.forPrimaryKey(id, version) {
            someInt(editDto.someInt)
            someString(editDto.someString)
        }

        setFields(updater)

    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun updateSomeInt(editDto: AlphaWithHistoryUpdate_someIntRequestDto) {

        val currentUsername = CurrentUserHolder.currentUsername

        logger.info("BEGIN: updateSomeInt. currentUsername=${currentUsername}, dto=$editDto")

        val version = editDto.version

        val updater = AlphaWithHistoryEntityUpdater.forPrimaryKey(editDto.id, version) {
            someInt(editDto.someInt)
        }

        setFields(updater)

    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun updateSomeString(editDto: AlphaWithHistoryUpdate_someStringRequestDto) {

        val currentUsername = CurrentUserHolder.currentUsername

        logger.info("BEGIN: updateSomeString. currentUsername=${currentUsername}, dto=$editDto")

        val version = editDto.version

        val updater = AlphaWithHistoryEntityUpdater.forPrimaryKey(editDto.id, version) {
            someString(editDto.someString)
        }

        setFields(updater)

    }


    @Transactional
    fun setFields(updater: AlphaWithHistoryEntityUpdater): Int {

        val count = this.entityRepo.setFields(updater)
        this.alphaWithHistoryCrudNotifier.onEntityUpdated(updater.id)
        return count
        
    }


    @Transactional
    @PreAuthorize("hasAuthority('WRITE')")
    fun delete(id: DomainId) {

        if (this.bravoWithHistoryRepo.existsByAlpha(id)) {
            throw this.maiaProblems.foreignKeyRecordsExist("BravoWithHistory")
        }

        if (this.bravoWithHistoryHistoryRepo.existsByAlpha(id)) {
            throw this.maiaProblems.foreignKeyRecordsExist("BravoWithHistoryHistory")
        }

        val entityToDelete = this.entityRepo.findByPrimaryKeyOrNull(id)
                ?: return

        this.entityRepo.deleteByPrimaryKey(id)
        this.alphaWithHistoryCrudNotifier.onEntityDeleted(entityToDelete)

    }


}

