// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.showcase.user

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository


@Repository
class UserHistoryDao(
    private val fieldConverter: UserHistoryEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = UserHistoryEntityRowMapper()


    private val primaryKeyRowMapper = UserHistoryEntityPkRowMapper()


    fun insert(entity: UserHistoryEntity) {

        jdbcOps.update(
            """
            insert into maia.v_party_history (
                type_discriminator,
                authorities,
                change_type,
                created_by_id,
                created_timestamp_utc,
                encrypted_password,
                first_name,
                id,
                last_modified_by_id,
                last_modified_timestamp_utc,
                last_name,
                lifecycle_state,
                version
            ) values (
                'USR',
                :authorities,
                :changeType,
                :createdBy,
                :createdTimestampUtc,
                :encryptedPassword,
                :firstName,
                :id,
                :lastModifiedBy,
                :lastModifiedTimestampUtc,
                :lastName,
                :lifecycleState,
                :version
            )
            """.trimIndent(),
            SqlParams().apply {
                addListOfStrings("authorities", entity.authorities.map { it.name })
                addValue("changeType", entity.changeType)
                addValue("createdBy", entity.createdBy)
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("displayName", entity.displayName)
                addValue("encryptedPassword", entity.encryptedPassword)
                addValue("firstName", entity.firstName)
                addValue("id", entity.id)
                addValue("lastModifiedBy", entity.lastModifiedBy)
                addValue("lastModifiedTimestampUtc", entity.lastModifiedTimestampUtc)
                addValue("lastName", entity.lastName)
                addValue("lifecycleState", entity.lifecycleState)
                addValue("version", entity.version)
            }
        )

    }


    fun bulkInsert(entities: List<UserHistoryEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into maia.v_party_history (
                type_discriminator,
                authorities,
                change_type,
                created_by_id,
                created_timestamp_utc,
                encrypted_password,
                first_name,
                id,
                last_modified_by_id,
                last_modified_timestamp_utc,
                last_name,
                lifecycle_state,
                version
            ) values (
                'USR',
                :authorities,
                :changeType,
                :createdBy,
                :createdTimestampUtc,
                :encryptedPassword,
                :firstName,
                :id,
                :lastModifiedBy,
                :lastModifiedTimestampUtc,
                :lastName,
                :lifecycleState,
                :version
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addListOfStrings("authorities", entity.authorities.map { it.name })
                    addValue("changeType", entity.changeType)
                    addValue("createdBy", entity.createdBy)
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("displayName", entity.displayName)
                    addValue("encryptedPassword", entity.encryptedPassword)
                    addValue("firstName", entity.firstName)
                    addValue("id", entity.id)
                    addValue("lastModifiedBy", entity.lastModifiedBy)
                    addValue("lastModifiedTimestampUtc", entity.lastModifiedTimestampUtc)
                    addValue("lastName", entity.lastName)
                    addValue("lifecycleState", entity.lifecycleState)
                    addValue("version", entity.version)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from maia.v_party_history",
            SqlParams()
        )

    }


    fun count(filter: UserHistoryEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from maia.v_party_history
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(primaryKey: UserHistoryEntityPk): UserHistoryEntity {

        return findByPrimaryKeyOrNull(primaryKey)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    UserHistoryEntity::class.java,
                    mapOf(
                        "id" to primaryKey.id,
                        "version" to primaryKey.version,
                    )
                ),
                UserHistoryEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(primaryKey: UserHistoryEntityPk): UserHistoryEntity? {

        return jdbcOps.queryForList(
            "select * from maia.v_party_history where id = :id and version = :version",
            SqlParams().apply {
                addValue("id", primaryKey.id)
                addValue("version", primaryKey.version)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: DomainId, version: Long): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from maia.v_party_history where id = :id and version = :version",
            SqlParams().apply {
                addValue("id", id)
                addValue("version", version)
           }
        )
       
        return count > 0
       
    }

    fun findAllBy(filter: UserHistoryEntityFilter): List<UserHistoryEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.v_party_history where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: UserHistoryEntityFilter): Sequence<UserHistoryEntityPk> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id, version from maia.v_party_history where $whereClause",
            sqlParams,
            this.primaryKeyRowMapper
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<UserHistoryEntityPk> {

        return this.jdbcOps.queryForSequence(
            "select id, version from maia.v_party_history;",
            SqlParams(),
            this.primaryKeyRowMapper
        )

    }


    fun findAllBy(filter: UserHistoryEntityFilter, pageable: Pageable): List<UserHistoryEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.v_party_history where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<UserHistoryEntity> {

        return this.jdbcOps.queryForSequence(
            "select * from maia.v_party_history;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


}

