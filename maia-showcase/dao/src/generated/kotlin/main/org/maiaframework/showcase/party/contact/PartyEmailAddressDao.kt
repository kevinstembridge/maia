// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.showcase.party.contact

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.domain.contact.EmailAddress
import org.maiaframework.domain.contact.EmailAddressPurpose
import org.maiaframework.domain.persist.FieldUpdate
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.MaiaRowMapper
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository
import java.time.Instant


@Repository
class PartyEmailAddressDao(
    private val fieldConverter: PartyEmailAddressEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = PartyEmailAddressEntityRowMapper()


    private val primaryKeyRowMapper = MaiaRowMapper { rsa -> rsa.readDomainId("id") }


    fun insert(entity: PartyEmailAddressEntity) {

        jdbcOps.update(
            """
            insert into maia.party_email_address (
                created_by_id,
                created_timestamp_utc,
                effective_range,
                email_address,
                id,
                is_primary_contact,
                last_modified_by_id,
                last_modified_timestamp_utc,
                party_id,
                purposes,
                version
            ) values (
                :createdBy,
                :createdTimestampUtc,
                tstzrange(:effectiveFrom, :effectiveTo),
                :emailAddress,
                :id,
                :isPrimaryContact,
                :lastModifiedBy,
                :lastModifiedTimestampUtc,
                :party,
                :purposes,
                :version
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdBy", entity.createdBy)
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("effectiveFrom", entity.effectiveFrom)
                addValue("effectiveTo", entity.effectiveTo)
                addValue("emailAddress", entity.emailAddress)
                addValue("id", entity.id)
                addValue("isPrimaryContact", entity.isPrimaryContact)
                addValue("lastModifiedBy", entity.lastModifiedBy)
                addValue("lastModifiedTimestampUtc", entity.lastModifiedTimestampUtc)
                addValue("party", entity.party)
                addListOfStrings("purposes", entity.purposes.map { it.name })
                addValue("version", entity.version)
            }
        )

    }


    fun bulkInsert(entities: List<PartyEmailAddressEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into maia.party_email_address (
                created_by_id,
                created_timestamp_utc,
                effective_range,
                email_address,
                id,
                is_primary_contact,
                last_modified_by_id,
                last_modified_timestamp_utc,
                party_id,
                purposes,
                version
            ) values (
                :createdBy,
                :createdTimestampUtc,
                tstzrange(:effectiveFrom, :effectiveTo),
                :emailAddress,
                :id,
                :isPrimaryContact,
                :lastModifiedBy,
                :lastModifiedTimestampUtc,
                :party,
                :purposes,
                :version
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("createdBy", entity.createdBy)
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("effectiveFrom", entity.effectiveFrom)
                    addValue("effectiveTo", entity.effectiveTo)
                    addValue("emailAddress", entity.emailAddress)
                    addValue("id", entity.id)
                    addValue("isPrimaryContact", entity.isPrimaryContact)
                    addValue("lastModifiedBy", entity.lastModifiedBy)
                    addValue("lastModifiedTimestampUtc", entity.lastModifiedTimestampUtc)
                    addValue("party", entity.party)
                    addListOfStrings("purposes", entity.purposes.map { it.name })
                    addValue("version", entity.version)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from maia.party_email_address",
            SqlParams()
        )

    }


    fun count(filter: PartyEmailAddressEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from maia.party_email_address
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(id: DomainId): PartyEmailAddressEntity {

        return findByPrimaryKeyOrNull(id)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    PartyEmailAddressEntity::class.java,
                    mapOf(
                        "id" to id,
                    )
                ),
                PartyEmailAddressEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(id: DomainId): PartyEmailAddressEntity? {

        return jdbcOps.queryForList(
            "select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.party_email_address where id = :id",
            SqlParams().apply {
                addValue("id", id)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from maia.party_email_address where id = :id",
            SqlParams().apply {
                addValue("id", id)
           }
        )
       
        return count > 0
       
    }

    fun findByParty(party: DomainId): List<PartyEmailAddressEntity> {

        return jdbcOps.queryForList(
            """
            select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.party_email_address
            where party_id = :party
            """.trimIndent(),
            SqlParams().apply {
                addValue("party", party)
            },
            this.entityRowMapper
        )

    }


    fun findByEmailAddress(emailAddress: EmailAddress): List<PartyEmailAddressEntity> {

        return jdbcOps.queryForList(
            """
            select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.party_email_address
            where email_address = :emailAddress
            """.trimIndent(),
            SqlParams().apply {
                addValue("emailAddress", emailAddress)
            },
            this.entityRowMapper
        )

    }


    fun findEffectiveByParty(party: DomainId): List<PartyEmailAddressEntity> {

        return jdbcOps.queryForList(
            """
            select 
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.party_email_address
            where party_id = :party
            and effective_range @> current_timestamp
            """.trimIndent(),
            SqlParams().apply {
                addValue("party", party)
            },
            this.entityRowMapper
        )

    }


    fun findEffectiveByEmailAddress(emailAddress: EmailAddress): List<PartyEmailAddressEntity> {

        return jdbcOps.queryForList(
            """
            select 
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.party_email_address
            where email_address = :emailAddress
            and effective_range @> current_timestamp
            """.trimIndent(),
            SqlParams().apply {
                addValue("emailAddress", emailAddress)
            },
            this.entityRowMapper
        )

    }


    fun findAllBy(filter: PartyEmailAddressEntityFilter): List<PartyEmailAddressEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            """
            select 
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.party_email_address
            where $whereClause
            """.trimIndent(),
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: PartyEmailAddressEntityFilter): Sequence<DomainId> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id from maia.party_email_address where $whereClause",
            sqlParams,
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<DomainId> {

        return this.jdbcOps.queryForSequence(
            "select id from maia.party_email_address;",
            SqlParams(),
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllBy(filter: PartyEmailAddressEntityFilter, pageable: Pageable): List<PartyEmailAddressEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            """
            select
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.party_email_address
            where $whereClause
            $orderByClause
            $limitClause
            $offsetClause
            """.trimIndent(),
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<PartyEmailAddressEntity> {

        return this.jdbcOps.queryForSequence(
            """
            select
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.party_email_address;
            """.trimIndent(),
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun existsByParty(party: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*)
            from maia.party_email_address
            where party_id = :party
            """.trimIndent(),
            SqlParams().apply {
                addValue("party", party)
            }
        )

        return count > 0

    }


    fun existsByEmailAddress(emailAddress: EmailAddress): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*)
            from maia.party_email_address
            where email_address = :emailAddress
            """.trimIndent(),
            SqlParams().apply {
                addValue("emailAddress", emailAddress)
            }
        )

        return count > 0

    }


    fun existsByCreatedBy(createdBy: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*)
            from maia.party_email_address
            where created_by_id = :createdBy
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdBy", createdBy)
            }
        )

        return count > 0

    }


    fun existsByLastModifiedBy(lastModifiedBy: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*)
            from maia.party_email_address
            where last_modified_by_id = :lastModifiedBy
            """.trimIndent(),
            SqlParams().apply {
                addValue("lastModifiedBy", lastModifiedBy)
            }
        )

        return count > 0

    }


    fun setFields(updaters: List<PartyEmailAddressEntityUpdater>) {

        updaters.forEach { setFields(it) }

    }


    fun setFields(updater: PartyEmailAddressEntityUpdater): Int {

        val sql = StringBuilder()
        val sqlParams = SqlParams()

        sql.append("update maia.party_email_address set ")

        val effectiveFromUpdate = updater.fields.find { it.classFieldName == "effectiveFrom" }
        val effectiveToUpdate = updater.fields.find { it.classFieldName == "effectiveTo" }

        val fieldClauses = updater.fields
            .filterNot { it.classFieldName == "effectiveFrom" || it.classFieldName == "effectiveTo" }
            .plus(FieldUpdate("version_incremented", "version", updater.version + 1))
            .map { field ->

                addField(field, sqlParams)
                "${field.dbColumnName} = :${field.classFieldName}"

            }
            .plus(
                when {
                    effectiveFromUpdate != null && effectiveToUpdate != null -> {
                        sqlParams.addValue("effectiveFrom", effectiveFromUpdate.value as Instant?)
                        sqlParams.addValue("effectiveTo", effectiveToUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(:effectiveFrom, :effectiveTo)")
                    }
                    effectiveFromUpdate != null -> {
                        sqlParams.addValue("effectiveFrom", effectiveFromUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(:effectiveFrom, upper(effective_range))")
                    }
                    effectiveToUpdate != null -> {
                        sqlParams.addValue("effectiveTo", effectiveToUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(lower(effective_range), :effectiveTo)")
                    }
                    else -> emptyList()
                }
            )
            .joinToString(", ")

        sql.append(fieldClauses)
        sql.append(" where id = :id")
        sql.append(" and version = :version")

        sqlParams.addValue("id", updater.id)

        sqlParams.addValue("version", updater.version)
        sqlParams.addValue("version_incremented", updater.version + 1)

        return this.jdbcOps.update(sql.toString(), sqlParams)

    }


    private fun addField(field: FieldUpdate, sqlParams: SqlParams) {

        when (field.classFieldName) {
            "emailAddress" -> sqlParams.addValue("emailAddress", (field.value as EmailAddress).value)
            "isPrimaryContact" -> sqlParams.addValue("isPrimaryContact", field.value as Boolean)
            "lastModifiedBy" -> sqlParams.addValue("lastModifiedBy", field.value as DomainId)
            "lastModifiedTimestampUtc" -> sqlParams.addValue("lastModifiedTimestampUtc", field.value as Instant)
            "party" -> sqlParams.addValue("party", field.value as DomainId)
            "purposes" -> sqlParams.addListOfStrings("purposes", field.value as List<EmailAddressPurpose>) { it.name }
        }

    }


    fun closeEffectiveRange(id: DomainId): Boolean {

        val updatedCount = this.jdbcOps.update(
            """
            update maia.party_email_address
            set effective_range = tstzrange(lower(effective_range), now())
            where id = :id
            """.trimIndent(),
            SqlParams().apply {
                addValue("id", id)
            }
        )

        return updatedCount > 0

    }


}

