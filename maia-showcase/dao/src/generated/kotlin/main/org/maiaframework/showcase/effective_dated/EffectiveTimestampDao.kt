// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.showcase.effective_dated

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.domain.persist.FieldUpdate
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.MaiaRowMapper
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository
import java.time.Instant


@Repository
class EffectiveTimestampDao(
    private val fieldConverter: EffectiveTimestampEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = EffectiveTimestampEntityRowMapper()


    private val primaryKeyRowMapper = MaiaRowMapper { rsa -> rsa.readDomainId("id") }


    fun insert(entity: EffectiveTimestampEntity) {

        jdbcOps.update(
            """
            insert into maia.effective_timestamp (
                created_timestamp_utc,
                effective_range,
                id,
                some_string
            ) values (
                :createdTimestampUtc,
                tstzrange(:effectiveFrom, :effectiveTo),
                :id,
                :someString
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("effectiveFrom", entity.effectiveFrom)
                addValue("effectiveTo", entity.effectiveTo)
                addValue("id", entity.id)
                addValue("someString", entity.someString)
            }
        )

    }


    fun bulkInsert(entities: List<EffectiveTimestampEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into maia.effective_timestamp (
                created_timestamp_utc,
                effective_range,
                id,
                some_string
            ) values (
                :createdTimestampUtc,
                tstzrange(:effectiveFrom, :effectiveTo),
                :id,
                :someString
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("effectiveFrom", entity.effectiveFrom)
                    addValue("effectiveTo", entity.effectiveTo)
                    addValue("id", entity.id)
                    addValue("someString", entity.someString)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from maia.effective_timestamp",
            SqlParams()
        )

    }


    fun count(filter: EffectiveTimestampEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from maia.effective_timestamp
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(id: DomainId): EffectiveTimestampEntity {

        return findByPrimaryKeyOrNull(id)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    EffectiveTimestampEntity::class.java,
                    mapOf(
                        "id" to id,
                    )
                ),
                EffectiveTimestampEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(id: DomainId): EffectiveTimestampEntity? {

        return jdbcOps.queryForList(
            "select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.effective_timestamp where id = :id",
            SqlParams().apply {
            addValue("id", id)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from maia.effective_timestamp where id = :id",
            SqlParams().apply {
                addValue("id", id)
           }
        )
       
        return count > 0
       
    }

    fun findBySomeString(someString: String): List<EffectiveTimestampEntity> {

        return jdbcOps.queryForList(
            """
            select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.effective_timestamp
            where some_string = :someString
            """.trimIndent(),
            SqlParams().apply {
                addValue("someString", someString)
            },
            this.entityRowMapper
        )

    }


    fun findEffectiveBySomeString(someString: String): EffectiveTimestampEntity? {

        return jdbcOps.queryForObjectOrNull(
            """
            select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.effective_timestamp
            where some_string = :someString
            and effective_range @> current_timestamp
            """.trimIndent(),
            SqlParams().apply {
                addValue("someString", someString)
            },
            this.entityRowMapper
        )

    }


    fun findAllBy(filter: EffectiveTimestampEntityFilter): List<EffectiveTimestampEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.effective_timestamp where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: EffectiveTimestampEntityFilter): Sequence<DomainId> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id from maia.effective_timestamp where $whereClause",
            sqlParams,
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<DomainId> {

        return this.jdbcOps.queryForSequence(
            "select id from maia.effective_timestamp;",
            SqlParams(),
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllBy(filter: EffectiveTimestampEntityFilter, pageable: Pageable): List<EffectiveTimestampEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.effective_timestamp where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<EffectiveTimestampEntity> {

        return this.jdbcOps.queryForSequence(
            "select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.effective_timestamp;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun setFields(updaters: List<EffectiveTimestampEntityUpdater>) {

        updaters.forEach { setFields(it) }

    }


    fun setFields(updater: EffectiveTimestampEntityUpdater): Int {

        val sql = StringBuilder()
        val sqlParams = SqlParams()

        sql.append("update maia.effective_timestamp set ")

        val effectiveFromUpdate = updater.fields.find { it.classFieldName == "effectiveFrom" }
        val effectiveToUpdate = updater.fields.find { it.classFieldName == "effectiveTo" }

        val fieldClauses = updater.fields
            .filterNot { it.classFieldName == "effectiveFrom" || it.classFieldName == "effectiveTo" }
            .map { field ->

                addField(field, sqlParams)
                "${field.dbColumnName} = :${field.classFieldName}"

            }
            .plus(
                when {
                    effectiveFromUpdate != null && effectiveToUpdate != null -> {
                        sqlParams.addValue("effectiveFrom", effectiveFromUpdate.value as Instant?)
                        sqlParams.addValue("effectiveTo", effectiveToUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(:effectiveFrom, :effectiveTo)")
                    }
                    effectiveFromUpdate != null -> {
                        sqlParams.addValue("effectiveFrom", effectiveFromUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(:effectiveFrom, upper(effective_range))")
                    }
                    effectiveToUpdate != null -> {
                        sqlParams.addValue("effectiveTo", effectiveToUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(lower(effective_range), :effectiveTo)")
                    }
                    else -> emptyList()
                }
            )
            .joinToString(", ")

        sql.append(fieldClauses)
        sql.append(" where id = :id")

        sqlParams.addValue("id", updater.id)

        return this.jdbcOps.update(sql.toString(), sqlParams)

    }


    private fun addField(field: FieldUpdate, sqlParams: SqlParams) {

        when (field.classFieldName) {
        }

    }


    fun deleteByPrimaryKey(id: DomainId): Boolean {

        val existingEntity = findByPrimaryKeyOrNull(id) ?: return false

        val deletedCount = this.jdbcOps.update(
            "delete from maia.effective_timestamp where id = :id",
            SqlParams().apply {
                addValue("id", id)
            }
        )

        return deletedCount > 0

    }


    fun removeByPrimaryKey(id: DomainId): EffectiveTimestampEntity? {

        val found = findByPrimaryKeyOrNull(id)

        if (found != null) {
            deleteByPrimaryKey(id)
        }

        return found

    }


    fun deleteAll() {
        this.jdbcOps.update("delete from maia.effective_timestamp")
    }


}

