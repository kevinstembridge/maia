// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.showcase.party.contact

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.domain.persist.FieldUpdate
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.MaiaRowMapper
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository
import java.time.Instant


@Repository
class EmailAddressVerificationDao(
    private val fieldConverter: EmailAddressVerificationEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = EmailAddressVerificationEntityRowMapper()


    private val primaryKeyRowMapper = MaiaRowMapper { rsa -> rsa.readDomainId("id") }


    fun insert(entity: EmailAddressVerificationEntity) {

        jdbcOps.update(
            """
            insert into maia.email_address_verification (
                created_by_id,
                created_timestamp_utc,
                effective_from,
                effective_to,
                email_address_id,
                id,
                ip_address,
                last_modified_by_id,
                last_modified_timestamp_utc,
                version
            ) values (
                :createdBy,
                :createdTimestampUtc,
                :effectiveFrom,
                :effectiveTo,
                :emailAddressId,
                :id,
                :ipAddress,
                :lastModifiedBy,
                :lastModifiedTimestampUtc,
                :version
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdBy", entity.createdBy)
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("effectiveFrom", entity.effectiveFrom)
                addValue("effectiveTo", entity.effectiveTo)
                addValue("emailAddressId", entity.emailAddressId)
                addValue("id", entity.id)
                addValue("ipAddress", entity.ipAddress)
                addValue("lastModifiedBy", entity.lastModifiedBy)
                addValue("lastModifiedTimestampUtc", entity.lastModifiedTimestampUtc)
                addValue("version", entity.version)
            }
        )

    }


    fun bulkInsert(entities: List<EmailAddressVerificationEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into maia.email_address_verification (
                created_by_id,
                created_timestamp_utc,
                effective_from,
                effective_to,
                email_address_id,
                id,
                ip_address,
                last_modified_by_id,
                last_modified_timestamp_utc,
                version
            ) values (
                :createdBy,
                :createdTimestampUtc,
                :effectiveFrom,
                :effectiveTo,
                :emailAddressId,
                :id,
                :ipAddress,
                :lastModifiedBy,
                :lastModifiedTimestampUtc,
                :version
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("createdBy", entity.createdBy)
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("effectiveFrom", entity.effectiveFrom)
                    addValue("effectiveTo", entity.effectiveTo)
                    addValue("emailAddressId", entity.emailAddressId)
                    addValue("id", entity.id)
                    addValue("ipAddress", entity.ipAddress)
                    addValue("lastModifiedBy", entity.lastModifiedBy)
                    addValue("lastModifiedTimestampUtc", entity.lastModifiedTimestampUtc)
                    addValue("version", entity.version)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from maia.email_address_verification",
            SqlParams()
        )

    }


    fun count(filter: EmailAddressVerificationEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from maia.email_address_verification
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(id: DomainId): EmailAddressVerificationEntity {

        return findByPrimaryKeyOrNull(id)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    EmailAddressVerificationEntity::class.java,
                    mapOf(
                        "id" to id,
                    )
                ),
                EmailAddressVerificationEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(id: DomainId): EmailAddressVerificationEntity? {

        return jdbcOps.queryForList(
            "select * from maia.email_address_verification where id = :id",
            SqlParams().apply {
            addValue("id", id)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from maia.email_address_verification where id = :id",
            SqlParams().apply {
                addValue("id", id)
           }
        )
       
        return count > 0
       
    }

    fun findAllBy(filter: EmailAddressVerificationEntityFilter): List<EmailAddressVerificationEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.email_address_verification where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: EmailAddressVerificationEntityFilter): Sequence<DomainId> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id from maia.email_address_verification where $whereClause",
            sqlParams,
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<DomainId> {

        return this.jdbcOps.queryForSequence(
            "select id from maia.email_address_verification;",
            SqlParams(),
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllBy(filter: EmailAddressVerificationEntityFilter, pageable: Pageable): List<EmailAddressVerificationEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.email_address_verification where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<EmailAddressVerificationEntity> {

        return this.jdbcOps.queryForSequence(
            "select * from maia.email_address_verification;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun existsByCreatedBy(createdBy: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*) from maia.email_address_verification
            where created_by_id = :createdBy
            """.trimIndent(),
            SqlParams().apply {
            addValue("createdBy", createdBy)
            }
        )

        return count > 0

    }


    fun existsByLastModifiedBy(lastModifiedBy: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*) from maia.email_address_verification
            where last_modified_by_id = :lastModifiedBy
            """.trimIndent(),
            SqlParams().apply {
            addValue("lastModifiedBy", lastModifiedBy)
            }
        )

        return count > 0

    }


    fun setFields(updaters: List<EmailAddressVerificationEntityUpdater>) {

        updaters.forEach { setFields(it) }

    }


    fun setFields(updater: EmailAddressVerificationEntityUpdater): Int {

        val sql = StringBuilder()
        val sqlParams = SqlParams()

        sql.append("update maia.email_address_verification set ")

        val fieldClauses = updater.fields
            .plus(FieldUpdate("version_incremented", "version", updater.version + 1))
            .map { field ->

                addField(field, sqlParams)
                "${field.dbColumnName} = :${field.classFieldName}"

            }.joinToString(", ")

        sql.append(fieldClauses)
        sql.append(" where id = :id")
        sql.append(" and version = :version")

        sqlParams.addValue("id", updater.id)

        sqlParams.addValue("version", updater.version)
        sqlParams.addValue("version_incremented", updater.version + 1)

        return this.jdbcOps.update(sql.toString(), sqlParams)

    }


    private fun addField(field: FieldUpdate, sqlParams: SqlParams) {

        when (field.classFieldName) {
            "effectiveFrom" -> sqlParams.addValue("effectiveFrom", field.value as Instant?)
            "effectiveTo" -> sqlParams.addValue("effectiveTo", field.value as Instant?)
            "lastModifiedBy" -> sqlParams.addValue("lastModifiedBy", field.value as DomainId?)
            "lastModifiedTimestampUtc" -> sqlParams.addValue("lastModifiedTimestampUtc", field.value as Instant)
        }

    }


}

