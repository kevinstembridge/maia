// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.showcase.user

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository


@Repository
class UserGroupMembershipHistoryDao(
    private val fieldConverter: UserGroupMembershipHistoryEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = UserGroupMembershipHistoryEntityRowMapper()


    private val primaryKeyRowMapper = UserGroupMembershipHistoryEntityPkRowMapper()


    fun insert(entity: UserGroupMembershipHistoryEntity) {

        jdbcOps.update(
            """
            insert into maia.user_group_membership_history (
                change_type,
                created_timestamp_utc,
                id,
                user_group_id,
                user_id,
                version
            ) values (
                :changeType,
                :createdTimestampUtc,
                :id,
                :userGroupId,
                :userId,
                :version
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("changeType", entity.changeType)
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("id", entity.id)
                addValue("userGroupId", entity.userGroupId)
                addValue("userId", entity.userId)
                addValue("version", entity.version)
            }
        )

    }


    fun bulkInsert(entities: List<UserGroupMembershipHistoryEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into maia.user_group_membership_history (
                change_type,
                created_timestamp_utc,
                id,
                user_group_id,
                user_id,
                version
            ) values (
                :changeType,
                :createdTimestampUtc,
                :id,
                :userGroupId,
                :userId,
                :version
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("changeType", entity.changeType)
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("id", entity.id)
                    addValue("userGroupId", entity.userGroupId)
                    addValue("userId", entity.userId)
                    addValue("version", entity.version)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from maia.user_group_membership_history",
            SqlParams()
        )

    }


    fun count(filter: UserGroupMembershipHistoryEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from maia.user_group_membership_history
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(primaryKey: UserGroupMembershipHistoryEntityPk): UserGroupMembershipHistoryEntity {

        return findByPrimaryKeyOrNull(primaryKey)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    UserGroupMembershipHistoryEntity::class.java,
                    mapOf(
                        "id" to primaryKey.id,
                        "version" to primaryKey.version,
                    )
                ),
                UserGroupMembershipHistoryEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(primaryKey: UserGroupMembershipHistoryEntityPk): UserGroupMembershipHistoryEntity? {

        return jdbcOps.queryForList(
            "select * from maia.user_group_membership_history where id = :id and version = :version",
            SqlParams().apply {
                addValue("id", primaryKey.id)
                addValue("version", primaryKey.version)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: DomainId, version: Long): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from maia.user_group_membership_history where id = :id and version = :version",
            SqlParams().apply {
                addValue("id", id)
                addValue("version", version)
           }
        )
       
        return count > 0
       
    }

    fun findAllBy(filter: UserGroupMembershipHistoryEntityFilter): List<UserGroupMembershipHistoryEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.user_group_membership_history where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: UserGroupMembershipHistoryEntityFilter): Sequence<UserGroupMembershipHistoryEntityPk> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id, version from maia.user_group_membership_history where $whereClause",
            sqlParams,
            this.primaryKeyRowMapper
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<UserGroupMembershipHistoryEntityPk> {

        return this.jdbcOps.queryForSequence(
            "select id, version from maia.user_group_membership_history;",
            SqlParams(),
            this.primaryKeyRowMapper
        )

    }


    fun findAllBy(filter: UserGroupMembershipHistoryEntityFilter, pageable: Pageable): List<UserGroupMembershipHistoryEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.user_group_membership_history where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<UserGroupMembershipHistoryEntity> {

        return this.jdbcOps.queryForSequence(
            "select * from maia.user_group_membership_history;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun existsByUserGroupId(userGroupId: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*) from maia.user_group_membership_history
            where user_group_id = :userGroupId
            """.trimIndent(),
            SqlParams().apply {
            addValue("userGroupId", userGroupId)
            }
        )

        return count > 0

    }


    fun existsByUserId(userId: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*) from maia.user_group_membership_history
            where user_id = :userId
            """.trimIndent(),
            SqlParams().apply {
            addValue("userId", userId)
            }
        )

        return count > 0

    }


}

