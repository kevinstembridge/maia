// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.showcase.join

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.domain.persist.FieldUpdate
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.MaiaRowMapper
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository


@Repository
class BravoDao(
    private val fieldConverter: BravoEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = BravoEntityRowMapper()


    private val primaryKeyRowMapper = MaiaRowMapper { rsa -> rsa.readDomainId("id") }


    private val fetchForEditDtoRowMapper = BravoFetchForEditDtoRowMapper()


    fun insert(entity: BravoEntity) {

        jdbcOps.update(
            """
            insert into maia.bravo (
                alpha_id,
                created_timestamp_utc,
                id,
                some_int,
                some_string
            ) values (
                :alpha,
                :createdTimestampUtc,
                :id,
                :someInt,
                :someString
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("alpha", entity.alpha)
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("id", entity.id)
                addValue("someInt", entity.someInt)
                addValue("someString", entity.someString)
            }
        )

    }


    fun bulkInsert(entities: List<BravoEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into maia.bravo (
                alpha_id,
                created_timestamp_utc,
                id,
                some_int,
                some_string
            ) values (
                :alpha,
                :createdTimestampUtc,
                :id,
                :someInt,
                :someString
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("alpha", entity.alpha)
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("id", entity.id)
                    addValue("someInt", entity.someInt)
                    addValue("someString", entity.someString)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from maia.bravo",
            SqlParams()
        )

    }


    fun count(filter: BravoEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from maia.bravo
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(id: DomainId): BravoEntity {

        return findByPrimaryKeyOrNull(id)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    BravoEntity::class.java,
                    mapOf(
                        "id" to id,
                    )
                ),
                BravoEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(id: DomainId): BravoEntity? {

        return jdbcOps.queryForList(
            "select * from maia.bravo where id = :id",
            SqlParams().apply {
            addValue("id", id)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from maia.bravo where id = :id",
            SqlParams().apply {
                addValue("id", id)
           }
        )
       
        return count > 0
       
    }

    fun findAllBy(filter: BravoEntityFilter): List<BravoEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.bravo where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: BravoEntityFilter): Sequence<DomainId> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id from maia.bravo where $whereClause",
            sqlParams,
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<DomainId> {

        return this.jdbcOps.queryForSequence(
            "select id from maia.bravo;",
            SqlParams(),
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllBy(filter: BravoEntityFilter, pageable: Pageable): List<BravoEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.bravo where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<BravoEntity> {

        return this.jdbcOps.queryForSequence(
            "select * from maia.bravo;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun existsByAlpha(alpha: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*) from maia.bravo
            where alpha_id = :alpha
            """.trimIndent(),
            SqlParams().apply {
            addValue("alpha", alpha)
            }
        )

        return count > 0

    }


    fun fetchForEdit(id: DomainId): BravoFetchForEditDto {

        return this.jdbcOps.queryForList(
            """
            select
                maia.alpha.id as alphaId,
                maia.alpha.some_string as alphaName,
                maia.bravo.created_timestamp_utc as createdTimestampUtc,
                maia.bravo.id as id,
                maia.bravo.some_int as someInt,
                maia.bravo.some_string as someString
            from maia.bravo
            join maia.alpha
                on maia.alpha.id = maia.bravo.alpha_id
            where maia.bravo.id = :id
            """,
            SqlParams().apply {
                addValue("id", id)
            },
            this.fetchForEditDtoRowMapper
        ).firstOrNull()
            ?: throw EntityNotFoundException(EntityClassAndPk(BravoEntity::class.java, mapOf("id" to id)), BravoEntityMeta.TABLE_NAME)

    }


    fun setFields(updaters: List<BravoEntityUpdater>) {

        updaters.forEach { setFields(it) }

    }


    fun setFields(updater: BravoEntityUpdater): Int {

        val sql = StringBuilder()
        val sqlParams = SqlParams()

        sql.append("update maia.bravo set ")

        val fieldClauses = updater.fields
            .map { field ->

                addField(field, sqlParams)
                "${field.dbColumnName} = :${field.classFieldName}"

            }.joinToString(", ")

        sql.append(fieldClauses)
        sql.append(" where id = :id")

        sqlParams.addValue("id", updater.id)

        return this.jdbcOps.update(sql.toString(), sqlParams)

    }


    private fun addField(field: FieldUpdate, sqlParams: SqlParams) {

        when (field.classFieldName) {
            "someInt" -> sqlParams.addValue("someInt", field.value as Int)
            "someString" -> sqlParams.addValue("someString", field.value as String)
        }

    }


    fun deleteByPrimaryKey(id: DomainId): Boolean {

        val existingEntity = findByPrimaryKeyOrNull(id) ?: return false

        val deletedCount = this.jdbcOps.update(
            "delete from maia.bravo where id = :id",
            SqlParams().apply {
                addValue("id", id)
            }
        )

        return deletedCount > 0

    }


    fun removeByPrimaryKey(id: DomainId): BravoEntity? {

        val found = findByPrimaryKeyOrNull(id)

        if (found != null) {
            deleteByPrimaryKey(id)
        }

        return found

    }


    fun deleteAll() {
        this.jdbcOps.update("delete from maia.bravo")
    }


}

