// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.showcase.many_to_many

import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.domain.persist.FieldUpdate
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.MaiaRowMapper
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository
import java.time.Instant


@Repository
class LeftToRightComplexDao(
    private val fieldConverter: LeftToRightComplexEntityFieldConverter,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = LeftToRightComplexEntityRowMapper()


    private val primaryKeyRowMapper = MaiaRowMapper { rsa -> rsa.readDomainId("id") }


    fun insert(entity: LeftToRightComplexEntity) {

        jdbcOps.update(
            """
            insert into maia.left_to_right_complex (
                created_timestamp_utc,
                effective_range,
                id,
                last_modified_timestamp_utc,
                left_id,
                right_id,
                some_int_on_complex
            ) values (
                :createdTimestampUtc,
                tstzrange(:effectiveFrom, :effectiveTo),
                :id,
                :lastModifiedTimestampUtc,
                :left,
                :right,
                :someIntOnComplex
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("effectiveFrom", entity.effectiveFrom)
                addValue("effectiveTo", entity.effectiveTo)
                addValue("id", entity.id)
                addValue("lastModifiedTimestampUtc", entity.lastModifiedTimestampUtc)
                addValue("left", entity.left)
                addValue("right", entity.right)
                addValue("someIntOnComplex", entity.someIntOnComplex)
            }
        )

    }


    fun bulkInsert(entities: List<LeftToRightComplexEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into maia.left_to_right_complex (
                created_timestamp_utc,
                effective_range,
                id,
                last_modified_timestamp_utc,
                left_id,
                right_id,
                some_int_on_complex
            ) values (
                :createdTimestampUtc,
                tstzrange(:effectiveFrom, :effectiveTo),
                :id,
                :lastModifiedTimestampUtc,
                :left,
                :right,
                :someIntOnComplex
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("effectiveFrom", entity.effectiveFrom)
                    addValue("effectiveTo", entity.effectiveTo)
                    addValue("id", entity.id)
                    addValue("lastModifiedTimestampUtc", entity.lastModifiedTimestampUtc)
                    addValue("left", entity.left)
                    addValue("right", entity.right)
                    addValue("someIntOnComplex", entity.someIntOnComplex)
                }
            }
        )

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from maia.left_to_right_complex",
            SqlParams()
        )

    }


    fun count(filter: LeftToRightComplexEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from maia.left_to_right_complex
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(id: DomainId): LeftToRightComplexEntity {

        return findByPrimaryKeyOrNull(id)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    LeftToRightComplexEntity::class.java,
                    mapOf(
                        "id" to id,
                    )
                ),
                LeftToRightComplexEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(id: DomainId): LeftToRightComplexEntity? {

        return jdbcOps.queryForList(
            "select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.left_to_right_complex where id = :id",
            SqlParams().apply {
                addValue("id", id)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(id: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from maia.left_to_right_complex where id = :id",
            SqlParams().apply {
                addValue("id", id)
           }
        )
       
        return count > 0
       
    }

    fun findByLeft(left: DomainId): List<LeftToRightComplexEntity> {

        return jdbcOps.queryForList(
            """
            select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.left_to_right_complex
            where left_id = :left
            """.trimIndent(),
            SqlParams().apply {
                addValue("left", left)
            },
            this.entityRowMapper
        )

    }


    fun findByRight(right: DomainId): List<LeftToRightComplexEntity> {

        return jdbcOps.queryForList(
            """
            select *, lower(effective_range) as effective_from, upper(effective_range) as effective_to from maia.left_to_right_complex
            where right_id = :right
            """.trimIndent(),
            SqlParams().apply {
                addValue("right", right)
            },
            this.entityRowMapper
        )

    }


    fun findEffectiveByLeft(left: DomainId): List<LeftToRightComplexEntity> {

        return jdbcOps.queryForList(
            """
            select 
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.left_to_right_complex
            where left_id = :left
            and effective_range @> current_timestamp
            """.trimIndent(),
            SqlParams().apply {
                addValue("left", left)
            },
            this.entityRowMapper
        )

    }


    fun findEffectiveByRight(right: DomainId): List<LeftToRightComplexEntity> {

        return jdbcOps.queryForList(
            """
            select 
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.left_to_right_complex
            where right_id = :right
            and effective_range @> current_timestamp
            """.trimIndent(),
            SqlParams().apply {
                addValue("right", right)
            },
            this.entityRowMapper
        )

    }


    fun findAllBy(filter: LeftToRightComplexEntityFilter): List<LeftToRightComplexEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            """
            select 
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.left_to_right_complex
            where $whereClause
            """.trimIndent(),
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: LeftToRightComplexEntityFilter): Sequence<DomainId> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select id from maia.left_to_right_complex where $whereClause",
            sqlParams,
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<DomainId> {

        return this.jdbcOps.queryForSequence(
            "select id from maia.left_to_right_complex;",
            SqlParams(),
            { rsa -> rsa.readDomainId("id") }
        )

    }


    fun findAllBy(filter: LeftToRightComplexEntityFilter, pageable: Pageable): List<LeftToRightComplexEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            """
            select
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.left_to_right_complex
            where $whereClause
            $orderByClause
            $limitClause
            $offsetClause
            """.trimIndent(),
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<LeftToRightComplexEntity> {

        return this.jdbcOps.queryForSequence(
            """
            select
                *,
                lower(effective_range) as effective_from,
                upper(effective_range) as effective_to
            from maia.left_to_right_complex;
            """.trimIndent(),
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun existsByLeft(left: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*)
            from maia.left_to_right_complex
            where left_id = :left
            """.trimIndent(),
            SqlParams().apply {
                addValue("left", left)
            }
        )

        return count > 0

    }


    fun existsByRight(right: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*)
            from maia.left_to_right_complex
            where right_id = :right
            """.trimIndent(),
            SqlParams().apply {
                addValue("right", right)
            }
        )

        return count > 0

    }


    fun setFields(updaters: List<LeftToRightComplexEntityUpdater>) {

        updaters.forEach { setFields(it) }

    }


    fun setFields(updater: LeftToRightComplexEntityUpdater): Int {

        val sql = StringBuilder()
        val sqlParams = SqlParams()

        sql.append("update maia.left_to_right_complex set ")

        val effectiveFromUpdate = updater.fields.find { it.classFieldName == "effectiveFrom" }
        val effectiveToUpdate = updater.fields.find { it.classFieldName == "effectiveTo" }

        val fieldClauses = updater.fields
            .filterNot { it.classFieldName == "effectiveFrom" || it.classFieldName == "effectiveTo" }
            .map { field ->

                addField(field, sqlParams)
                "${field.dbColumnName} = :${field.classFieldName}"

            }
            .plus(
                when {
                    effectiveFromUpdate != null && effectiveToUpdate != null -> {
                        sqlParams.addValue("effectiveFrom", effectiveFromUpdate.value as Instant?)
                        sqlParams.addValue("effectiveTo", effectiveToUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(:effectiveFrom, :effectiveTo)")
                    }
                    effectiveFromUpdate != null -> {
                        sqlParams.addValue("effectiveFrom", effectiveFromUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(:effectiveFrom, upper(effective_range))")
                    }
                    effectiveToUpdate != null -> {
                        sqlParams.addValue("effectiveTo", effectiveToUpdate.value as Instant?)
                        listOf("effective_range = tstzrange(lower(effective_range), :effectiveTo)")
                    }
                    else -> emptyList()
                }
            )
            .joinToString(", ")

        sql.append(fieldClauses)
        sql.append(" where id = :id")

        sqlParams.addValue("id", updater.id)

        return this.jdbcOps.update(sql.toString(), sqlParams)

    }


    private fun addField(field: FieldUpdate, sqlParams: SqlParams) {

        when (field.classFieldName) {
            "lastModifiedTimestampUtc" -> sqlParams.addValue("lastModifiedTimestampUtc", field.value as Instant)
            "left" -> sqlParams.addValue("left", field.value as DomainId)
            "right" -> sqlParams.addValue("right", field.value as DomainId)
            "someIntOnComplex" -> sqlParams.addValue("someIntOnComplex", field.value as Int)
        }

    }


    fun closeEffectiveRange(id: DomainId): Boolean {

        val updatedCount = this.jdbcOps.update(
            """
            update maia.left_to_right_complex
            set effective_range = tstzrange(lower(effective_range), now())
            where id = :id
            """.trimIndent(),
            SqlParams().apply {
                addValue("id", id)
            }
        )

        return updatedCount > 0

    }


    fun deleteByPrimaryKey(id: DomainId): Boolean {

        val existingEntity = findByPrimaryKeyOrNull(id) ?: return false

        val deletedCount = this.jdbcOps.update(
            "delete from maia.left_to_right_complex where id = :id",
            SqlParams().apply {
                addValue("id", id)
            }
        )

        return deletedCount > 0

    }


    fun removeByPrimaryKey(id: DomainId): LeftToRightComplexEntity? {

        val found = findByPrimaryKeyOrNull(id)

        if (found != null) {
            deleteByPrimaryKey(id)
        }

        return found

    }


    fun deleteAll() {
        this.jdbcOps.update("delete from maia.left_to_right_complex")
    }


}

