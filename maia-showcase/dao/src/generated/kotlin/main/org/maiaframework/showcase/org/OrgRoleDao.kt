// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.showcase.org

import la.org.OrgRoleKey
import org.maiaframework.domain.ChangeType
import org.maiaframework.domain.DomainId
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.domain.persist.FieldUpdate
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.MaiaRowMapper
import org.maiaframework.jdbc.OptimisticLockingException
import org.maiaframework.jdbc.ResultSetAdapter
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository
import java.sql.PreparedStatement
import java.time.Instant


@Repository
class OrgRoleDao(
    private val fieldConverter: OrgRoleEntityFieldConverter,
    private val historyDao: OrgRoleHistoryDao,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = OrgRoleEntityRowMapper()


    private val primaryKeyRowMapper = MaiaRowMapper { rsa -> rsa.readString("key") { OrgRoleKey(it) } }


    fun insert(entity: OrgRoleEntity) {

        jdbcOps.update(
            """
            insert into maia.org_role (
                created_by_id,
                created_timestamp,
                description,
                display_name,
                key,
                last_modified_by_id,
                last_modified_timestamp,
                version
            ) values (
                :createdBy,
                :createdTimestamp,
                :description,
                :displayName,
                :key,
                :lastModifiedBy,
                :lastModifiedTimestamp,
                :version
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdBy", entity.createdBy)
                addValue("createdTimestamp", entity.createdTimestamp)
                addValue("description", entity.description)
                addValue("displayName", entity.displayName)
                addValue("key", entity.key)
                addValue("lastModifiedBy", entity.lastModifiedBy)
                addValue("lastModifiedTimestamp", entity.lastModifiedTimestamp)
                addValue("version", entity.version)
            }
        )

        insertHistory(entity, ChangeType.CREATE)

    }


    fun bulkInsert(entities: List<OrgRoleEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into maia.org_role (
                created_by_id,
                created_timestamp,
                description,
                display_name,
                key,
                last_modified_by_id,
                last_modified_timestamp,
                version
            ) values (
                :createdBy,
                :createdTimestamp,
                :description,
                :displayName,
                :key,
                :lastModifiedBy,
                :lastModifiedTimestamp,
                :version
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("createdBy", entity.createdBy)
                    addValue("createdTimestamp", entity.createdTimestamp)
                    addValue("description", entity.description)
                    addValue("displayName", entity.displayName)
                    addValue("key", entity.key)
                    addValue("lastModifiedBy", entity.lastModifiedBy)
                    addValue("lastModifiedTimestamp", entity.lastModifiedTimestamp)
                    addValue("version", entity.version)
                }
            }
        )

        bulkInsertHistory(entities, ChangeType.CREATE)

    }


    private fun insertHistory(entity: OrgRoleEntity, changeType: ChangeType) {

        insertHistory(entity, entity.version, changeType)

    }


    private fun insertHistory(entity: OrgRoleEntity, version: Long, changeType: ChangeType) {

        this.historyDao.insert(history(entity, version, changeType))

    }


    private fun bulkInsertHistory(entities: List<OrgRoleEntity>, changeType: ChangeType) {

        val historyEntities = entities.map { history(it, it.version, changeType) }
        this.historyDao.bulkInsert(historyEntities)

    }


    private fun history(
        entity: OrgRoleEntity,
        version: Long,
        changeType: ChangeType
    ): OrgRoleHistoryEntity {

        val createdBy = entity.createdBy
        val createdTimestamp = entity.createdTimestamp
        val description = entity.description
        val displayName = entity.displayName
        val key = entity.key
        val lastModifiedBy = entity.lastModifiedBy
        val lastModifiedTimestamp = entity.lastModifiedTimestamp

        return OrgRoleHistoryEntity(
                changeType,
                createdBy,
                createdTimestamp,
                description,
                displayName,
                key,
                lastModifiedBy,
                lastModifiedTimestamp,
                version)

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from maia.org_role",
            SqlParams()
        )

    }


    fun count(filter: OrgRoleEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from maia.org_role
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(key: OrgRoleKey): OrgRoleEntity {

        return findByPrimaryKeyOrNull(key)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    OrgRoleEntity::class.java,
                    mapOf(
                        "key" to key,
                    )
                ),
                OrgRoleEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(key: OrgRoleKey): OrgRoleEntity? {

        return jdbcOps.queryForList(
            "select * from maia.org_role where key = :key",
            SqlParams().apply {
                addValue("key", key)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(key: OrgRoleKey): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from maia.org_role where key = :key",
            SqlParams().apply {
                addValue("key", key)
           }
        )
       
        return count > 0
       
    }

    fun findAllBy(filter: OrgRoleEntityFilter): List<OrgRoleEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.org_role where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: OrgRoleEntityFilter): Sequence<OrgRoleKey> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select key from maia.org_role where $whereClause",
            sqlParams,
            this.primaryKeyRowMapper
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<OrgRoleKey> {

        return this.jdbcOps.queryForSequence(
            "select key from maia.org_role;",
            SqlParams(),
            this.primaryKeyRowMapper
        )

    }


    fun findAllBy(filter: OrgRoleEntityFilter, pageable: Pageable): List<OrgRoleEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from maia.org_role where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<OrgRoleEntity> {

        return this.jdbcOps.queryForSequence(
            "select * from maia.org_role;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun existsByCreatedBy(createdBy: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*)
            from maia.org_role
            where created_by_id = :createdBy
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdBy", createdBy)
            }
        )

        return count > 0

    }


    fun existsByLastModifiedBy(lastModifiedBy: DomainId): Boolean {

        val count = jdbcOps.queryForInt(
            """
            select count(*)
            from maia.org_role
            where last_modified_by_id = :lastModifiedBy
            """.trimIndent(),
            SqlParams().apply {
                addValue("lastModifiedBy", lastModifiedBy)
            }
        )

        return count > 0

    }


    fun upsertByKey(upsertEntity: OrgRoleEntity): OrgRoleEntity {

        val persistedEntity = jdbcOps.execute(
            """
            insert into maia.org_role (
                created_by_id,
                created_timestamp,
                description,
                display_name,
                key,
                last_modified_by_id,
                last_modified_timestamp,
                version
            ) values (
                :createdBy,
                :createdTimestamp,
                :description,
                :displayName,
                :key,
                :lastModifiedBy,
                :lastModifiedTimestamp,
                :version
            )
            on conflict (key)
            do update set
                last_modified_by_id = :lastModifiedBy,
                last_modified_timestamp = :lastModifiedTimestamp,
                version = maia.org_role.version + 1
            returning *;
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdBy", upsertEntity.createdBy)
                addValue("createdTimestamp", upsertEntity.createdTimestamp)
                addValue("description", upsertEntity.description)
                addValue("displayName", upsertEntity.displayName)
                addValue("key", upsertEntity.key)
                addValue("lastModifiedBy", upsertEntity.lastModifiedBy)
                addValue("lastModifiedTimestamp", upsertEntity.lastModifiedTimestamp)
                addValue("version", upsertEntity.version)
            },
            { ps: PreparedStatement ->
                val rs = ps.executeQuery()
                rs.next()
                entityRowMapper.mapRow(ResultSetAdapter(rs))
            }
        )

        val changeType = if (persistedEntity!!.primaryKey != upsertEntity.primaryKey) ChangeType.UPDATE else ChangeType.CREATE
        insertHistory(persistedEntity, persistedEntity.version, changeType)

        return persistedEntity!!

    }


    fun setFields(updaters: List<OrgRoleEntityUpdater>) {

        updaters.forEach { setFields(it) }

    }


    fun setFields(updater: OrgRoleEntityUpdater): Int {

        val sql = StringBuilder()
        val sqlParams = SqlParams()

        sql.append("update maia.org_role set ")

        val fieldClauses = updater.fields
            .plus(FieldUpdate("version_incremented", "version", updater.version + 1))
            .map { field ->

                addField(field, sqlParams)
                "${field.dbColumnName} = :${field.classFieldName}"

            }.joinToString(", ")

        sql.append(fieldClauses)
        sql.append(" where key = :key")
        sql.append(" and version = :version")

        sqlParams.addValue("key", updater.key)

        sqlParams.addValue("version", updater.version)
        sqlParams.addValue("version_incremented", updater.version + 1)

        val updateCount = this.jdbcOps.update(sql.toString(), sqlParams)

        if (updateCount == 0) {

            throw OptimisticLockingException(OrgRoleEntityMeta.TABLE_NAME, updater.primaryKeyMap, updater.version)

        } else {

            val updatedEntity = findByPrimaryKey(updater.key)
            insertHistory(updatedEntity, ChangeType.UPDATE)

        }

        return updateCount

    }


    private fun addField(field: FieldUpdate, sqlParams: SqlParams) {

        when (field.classFieldName) {
            "lastModifiedBy" -> sqlParams.addValue("lastModifiedBy", field.value as DomainId)
            "lastModifiedTimestamp" -> sqlParams.addValue("lastModifiedTimestamp", field.value as Instant)
        }

    }


}

