// This source was generated by the Maia Framework code generator
// Renderer class: class org.maiaframework.gen.renderers.JdbcDaoRenderer

package org.maiaframework.gen.testing.sample.non_surrogate_pk

import org.maiaframework.domain.ChangeType
import org.maiaframework.domain.EntityClassAndPk
import org.maiaframework.domain.persist.FieldUpdate
import org.maiaframework.gen.sample.types.SomeStringValueClass
import org.maiaframework.jdbc.EntityNotFoundException
import org.maiaframework.jdbc.JdbcOps
import org.maiaframework.jdbc.MaiaRowMapper
import org.maiaframework.jdbc.OptimisticLockingException
import org.maiaframework.jdbc.ResultSetAdapter
import org.maiaframework.jdbc.SqlParams
import org.springframework.data.domain.Pageable
import org.springframework.stereotype.Repository
import java.sql.PreparedStatement


@Repository
class NonSurrogatePrimaryKeyDao(
    private val fieldConverter: NonSurrogatePrimaryKeyEntityFieldConverter,
    private val historyDao: NonSurrogatePrimaryKeyHistoryDao,
    private val jdbcOps: JdbcOps
) {


    private val entityRowMapper = NonSurrogatePrimaryKeyEntityRowMapper()


    private val primaryKeyRowMapper = MaiaRowMapper { rsa -> rsa.readString("some_string") { SomeStringValueClass(it) } }


    fun insert(entity: NonSurrogatePrimaryKeyEntity) {

        jdbcOps.update(
            """
            insert into testing.non_surrogate_primary_key (
                created_timestamp_utc,
                some_modifiable_string,
                some_string,
                version
            ) values (
                :createdTimestampUtc,
                :someModifiableString,
                :someString,
                :version
            )
            """.trimIndent(),
            SqlParams().apply {
                addValue("createdTimestampUtc", entity.createdTimestampUtc)
                addValue("someModifiableString", entity.someModifiableString)
                addValue("someString", entity.someString.value)
                addValue("version", entity.version)
            }
        )

        insertHistory(entity, ChangeType.CREATE)

    }


    fun bulkInsert(entities: List<NonSurrogatePrimaryKeyEntity>) {

        jdbcOps.batchUpdate(
            """
            insert into testing.non_surrogate_primary_key (
                created_timestamp_utc,
                some_modifiable_string,
                some_string,
                version
            ) values (
                :createdTimestampUtc,
                :someModifiableString,
                :someString,
                :version
            )
            """.trimIndent(),
            entities.map { entity ->
                SqlParams().apply {
                    addValue("createdTimestampUtc", entity.createdTimestampUtc)
                    addValue("someModifiableString", entity.someModifiableString)
                    addValue("someString", entity.someString.value)
                    addValue("version", entity.version)
                }
            }
        )

        bulkInsertHistory(entities, ChangeType.CREATE)

    }


    private fun insertHistory(entity: NonSurrogatePrimaryKeyEntity, changeType: ChangeType) {

        insertHistory(entity, entity.version, changeType)

    }


    private fun insertHistory(entity: NonSurrogatePrimaryKeyEntity, version: Long, changeType: ChangeType) {

        this.historyDao.insert(history(entity, version, changeType))

    }


    private fun bulkInsertHistory(entities: List<NonSurrogatePrimaryKeyEntity>, changeType: ChangeType) {

        val historyEntities = entities.map { history(it, it.version, changeType) }
        this.historyDao.bulkInsert(historyEntities)

    }


    private fun history(
        entity: NonSurrogatePrimaryKeyEntity,
        version: Long,
        changeType: ChangeType
    ): NonSurrogatePrimaryKeyHistoryEntity {

        val createdTimestampUtc = entity.createdTimestampUtc
        val someModifiableString = entity.someModifiableString
        val someString = entity.someString

        return NonSurrogatePrimaryKeyHistoryEntity(
                changeType,
                createdTimestampUtc,
                someModifiableString,
                someString,
                version)

    }


    fun count(): Long {

        return jdbcOps.queryForLong(
            "select count(*) from testing.non_surrogate_primary_key",
            SqlParams()
        )

    }


    fun count(filter: NonSurrogatePrimaryKeyEntityFilter): Long {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return jdbcOps.queryForLong(
            """
            select count(*) from testing.non_surrogate_primary_key
            where $whereClause
            """.trimIndent(),
            sqlParams
        )

    }


    @Throws(EntityNotFoundException::class)
    fun findByPrimaryKey(someString: SomeStringValueClass): NonSurrogatePrimaryKeyEntity {

        return findByPrimaryKeyOrNull(someString)
            ?: throw EntityNotFoundException(
                EntityClassAndPk(
                    NonSurrogatePrimaryKeyEntity::class.java,
                    mapOf(
                        "someString" to someString,
                    )
                ),
                NonSurrogatePrimaryKeyEntityMeta.TABLE_NAME
            )

    }


    fun findByPrimaryKeyOrNull(someString: SomeStringValueClass): NonSurrogatePrimaryKeyEntity? {

        return jdbcOps.queryForList(
            "select * from testing.non_surrogate_primary_key where some_string = :someString",
            SqlParams().apply {
            addValue("someString", someString.value)
            },
            this.entityRowMapper
        ).firstOrNull()

    }


    fun existsByPrimaryKey(someString: SomeStringValueClass): Boolean {

        val count = jdbcOps.queryForInt(
            "select count(*) from testing.non_surrogate_primary_key where some_string = :someString",
            SqlParams().apply {
                addValue("someString", someString.value)
           }
        )
       
        return count > 0
       
    }

    fun findAllBy(filter: NonSurrogatePrimaryKeyEntityFilter): List<NonSurrogatePrimaryKeyEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from testing.non_surrogate_primary_key where $whereClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    fun findPrimaryKeysAsSequence(filter: NonSurrogatePrimaryKeyEntityFilter): Sequence<SomeStringValueClass> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForSequence(
            "select some_string from testing.non_surrogate_primary_key where $whereClause",
            sqlParams,
            this.primaryKeyRowMapper
        )

    }


    fun findAllPrimaryKeysAsSequence(): Sequence<SomeStringValueClass> {

        return this.jdbcOps.queryForSequence(
            "select some_string from testing.non_surrogate_primary_key;",
            SqlParams(),
            this.primaryKeyRowMapper
        )

    }


    fun findAllBy(filter: NonSurrogatePrimaryKeyEntityFilter, pageable: Pageable): List<NonSurrogatePrimaryKeyEntity> {

        val whereClause = filter.whereClause(this.fieldConverter)
        val orderByClause = orderByClauseFor(pageable)
        val limitClause = limitClauseFor(pageable)
        val offsetClause = offsetClauseFor(pageable)

        val sqlParams = SqlParams()

        filter.populateSqlParams(sqlParams)

        return this.jdbcOps.queryForList(
            "select * from testing.non_surrogate_primary_key where $whereClause $orderByClause $limitClause $offsetClause",
            sqlParams,
            this.entityRowMapper
        )

    }


    private fun orderByClauseFor(pageable: Pageable): String {

        val properties = pageable.sort.map { "${it.property} ${it.direction}" }.joinToString(", ")
        return "ORDER BY $properties"

    }


    private fun limitClauseFor(pageable: Pageable): String {

        return "LIMIT ${pageable.pageSize}"

    }


    private fun offsetClauseFor(pageable: Pageable): String {

        return "OFFSET ${pageable.offset}"

    }


    fun findAllAsSequence(): Sequence<NonSurrogatePrimaryKeyEntity> {

        return this.jdbcOps.queryForSequence(
            "select * from testing.non_surrogate_primary_key;",
            SqlParams(),
            this.entityRowMapper,
        )

    }


    fun upsertBySomeString(upsertEntity: NonSurrogatePrimaryKeyEntity): NonSurrogatePrimaryKeyEntity {

        val persistedEntity = jdbcOps.execute(
            """
            insert into testing.non_surrogate_primary_key (
                created_timestamp_utc,
                some_modifiable_string,
                some_string,
                version
            ) values (
                :createdTimestampUtc,
                :someModifiableString,
                :someString,
                :version
            )
            on conflict (some_string)
            do update set
                some_modifiable_string = :someModifiableString,
                version = testing.non_surrogate_primary_key.version + 1
            returning *;
            """.trimIndent(),
            SqlParams().apply {
            addValue("createdTimestampUtc", upsertEntity.createdTimestampUtc)
            addValue("someModifiableString", upsertEntity.someModifiableString)
            addValue("someString", upsertEntity.someString.value)
            addValue("version", upsertEntity.version)
            },
            { ps: PreparedStatement ->
                val rs = ps.executeQuery()
                rs.next()
                entityRowMapper.mapRow(ResultSetAdapter(rs))
            }
        )

        val changeType = if (persistedEntity!!.primaryKey != upsertEntity.primaryKey) ChangeType.UPDATE else ChangeType.CREATE
        insertHistory(persistedEntity, persistedEntity.version, changeType)

        return persistedEntity!!

    }


    fun setFields(updaters: List<NonSurrogatePrimaryKeyEntityUpdater>) {

        updaters.forEach { setFields(it) }

    }


    fun setFields(updater: NonSurrogatePrimaryKeyEntityUpdater): Int {

        val sql = StringBuilder()
        val sqlParams = SqlParams()

        sql.append("update testing.non_surrogate_primary_key set ")

        val fieldClauses = updater.fields
            .plus(FieldUpdate("version_incremented", "version", updater.version + 1))
            .map { field ->

                addField(field, sqlParams)
                "${field.dbColumnName} = :${field.classFieldName}"

            }.joinToString(", ")

        sql.append(fieldClauses)
        sql.append(" where some_string = :someString")
        sql.append(" and version = :version")

        sqlParams.addValue("someString", updater.someString.value)

        sqlParams.addValue("version", updater.version)
        sqlParams.addValue("version_incremented", updater.version + 1)

        val updateCount = this.jdbcOps.update(sql.toString(), sqlParams)

        if (updateCount == 0) {

            throw OptimisticLockingException(NonSurrogatePrimaryKeyEntityMeta.TABLE_NAME, updater.primaryKey, updater.version)

        } else {

            val updatedEntity = findByPrimaryKey(updater.someString)
            insertHistory(updatedEntity, ChangeType.UPDATE)

        }

        return updateCount

    }


    private fun addField(field: FieldUpdate, sqlParams: SqlParams) {

        when (field.classFieldName) {
            "someModifiableString" -> sqlParams.addValue("someModifiableString", field.value as String)
        }

    }


    fun deleteByPrimaryKey(someString: SomeStringValueClass): Boolean {

        val existingEntity = findByPrimaryKeyOrNull(someString)

        if (existingEntity == null) {
            return false
        }

        val deletedCount = this.jdbcOps.update(
            "delete from testing.non_surrogate_primary_key where some_string = :someString",
            SqlParams().apply {
                addValue("someString", someString.value)
            }
        )

        if (deletedCount > 0) {

            this.historyDao.insert(history(existingEntity, existingEntity.version + 1, ChangeType.DELETE))
        }

        return deletedCount > 0

    }


    fun removeByPrimaryKey(someString: SomeStringValueClass): NonSurrogatePrimaryKeyEntity? {

        val found = findByPrimaryKeyOrNull(someString)

        if (found != null) {
            deleteByPrimaryKey(someString)
        }

        return found

    }


    fun deleteAll() {
        this.jdbcOps.update("delete from testing.non_surrogate_primary_key")
    }


}

